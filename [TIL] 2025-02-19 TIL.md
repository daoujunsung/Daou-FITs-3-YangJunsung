# TIL: jQuery
## 학습 날짜: 2025년 2월 19일
## 오늘 내가 새롭게 배운 것들
**jQuery**
- JavaScript는 함수형 언어와 객체지향 언어의 특징을 모두 가지고 있고 prototype을 사용해 코드 작성이 쉽지 않음
- 이를 쉽게 하기 위해 나온 라이브러리
- 화면 제어, 이벤트 처리, 시각적인 처리(CSS)를 쉽게 가능
- AJAX 처리를 직관적으로 처리 가능
- CDN(Content Delivery Network) 방식: 라이브러리 링크를 통해 설치 없이 필요할 때 동적으로 라이브러리를 받아오는 방식
- 양식: $('문자열로 된 Selector 또는 문서 객체(Document Object)').method()
  - $() => jQuery 객체
  - method()는 이미 정해져 있음
- Selector
  - 전체 선택자 : '*' => 모든 Element
  - 타입 선택자 : 원하는 tag 명을 이용해서 선택
  - 아이디 선택자 : '#id' => 원하는 ID를 가진 Element 선택 (unique 한 선택)
  - 클래스 선택자 : '.class' => 원하는 class 를 가진 Element 선택 (Not Unique)
  - 자식 선택자, 후손 선택자 : '>', 공백(' ')으로 선택
  - 동위 선택자
    - '+' : 바로 다음에 나오는 형제 1개
    - '~' : 바로 다음에 나오는 형제 포함 나머지 전부
  - ':selected' : 선택된 Element만 선택 
  - 선택자는 합성해서 사용 가능
- AJAX
  - JSON(JavaScript Object Notation): 자바스크립트 객체 표현법, API에서 데이터를 보낼 때 사용하는 표준 양식
  - async: 동기 방식인지 비동기 방식인지, 기본값은 비동기(true)
  - url: 호출할 API의 url
  - type: GET 방식인지 POST 방식인지
  - data: 보내는 데이터, 입력 파라미터를 입력, 2개 이상인 경우 JavaScript 객체로 표현
  - dataType: 서버가 보내주는 결과 데이터 타입, 기본값은 json
  - success: 호출에 성공했을 때 할 것
  - error: 호출에 실패했을 때 할 것
