# TIL: 금융 시스템의 이해와 SQL 기초
## 학습 날짜: 2025년 1월 15일
## 오늘 내가 새롭게 배운 것들

**금융 시스템 주요 구성 요소**
- 은행 시스템: 계좌 관리, 대출, 결제 서비스
- 증권 시스템: 주식, 채권, 펀드 거래 및 관리
- 보험 시스템: 보험 가입, 계약 관리, 보상 처리
- 결제 시스템: 카드 결제, 온라인 송금, 전자 화폐

**금융 시스템의 역할**
- 자금의 원활한 흐름 지원
- 금융 거래의 투명성과 신뢰성 확보
- 고객과 금융 기관 간의 연결
- 경제 성장을 위한 자금 조달 및 분배

**은행 시스템**
- 고객 관리 시스템(CRM): 고객 정보, 계좌 관리
- 대출 관리 시스템: 대출 신청, 심사, 상환 관리
- 결제 처리 시스템: 송금, 카드 결제, 자동 이체
- 리스크 관리 시스템: 신용 평가, 부도 예측, 규제 준수

**증권 시스템**
- 거래 시스템: 주식 및 채권 거래 처리
- 결제 및 청산 시스템: 거래 완료 후 자금 및 증권의 이동 관리
- 포트폴리오 관리 시스템: 타자자의 자산 구성 관리
- 리스크 관리 시스템: 시장 위험, 신용 위험 평가

**보험 시스템**
- 보험 계약 관리: 가입자 정보, 보험 상품 정보 관리
- 청구 및 보상 시스템: 사고 접수, 보상 처리
- 고객 서비스 시스템: 상담, 문의 처리, 계약 갱신 지원
- 리스크 관리 시스템: 재보험, 자산 및 부채 관리 TIL: 금융 시스템의 이해와 SQL 기초

**ORDER BY**
- SQL에서 데이터를 정렬하는 데 사용되는 절
- 기본적으로 오름차순 정렬
- 내림차순 정렬 시 DESC 키워드 사용
- 하나 이상의 컬럼을 기준으로 정렬 가능 (우선 순위는 지정한 컬럼의 순서에 따름)
- Oracle에서 NULL 값은 최대값으로 간주
- NULLS FIRST, NULLS LAST로 명시적 설정 가능

**페이징(Paging)**
- 대량의 데이터에서 일정한 크기로 데이터를 나누어 보여주는 방법
- 성능 최적화와 사용자 경험 개선을 위해 중요

**페이징 쿼리 기본 개념**
- ROWNUM: 오라클에서 결과 집합의 각 행에 부여되는 번호, 슈도 컬럼(Pseudo-column)
- ROW_NUMBER() 함수: 오라클의 윈도우 함수로, 정렬 기준에 따라 고유 번호를 부여
- 작성 예시:
```
SELECT ROWNUM, column_list FROM (
  SELECT column_list
  FROM table_name
  ORDER BY REG_DATE DESC
)
WHERE ROWNUM BETWEEN :start_row AND :end_row;
```
```
  SELECT *  
  FROM (  
    SELECT ROW_NUMBER() OVER  
      (ORDER BY column_name) AS row_num,  
    column_list  
    FROM table_name  
  )  
  WHERE row_num BETWEEN :start_row AND :end_row;
```

**페이징 쿼리 작성 시 주의사항**
- 정렬 기준: ORDER BY는 페이징 쿼리에서 필수, 정렬 기준이 없으면 결과 순서가 비정상적으로 출력될 수 있음
- 성능 고려: 대규모 데이터셋에서 OFFSET 사용 시 성능 저하 가능, 인덱스를 적절히 설정하여 쿼리 성능 최적화
- 다양한 페이지 크기 테스트: 다양한 크기의 페이지를 고려하여 쿼리를 설계

**DISTINCT**
- SQL에서 중복된 데이터를 제거하여 고유한 값만 반환하는 키워드
- 데이터의 요약/집계에 유용
- 여러 개의 컬럼에 사용을 하면 모든 컬럼이 중복되는 경우 제거

**GROUP BY**
- 집계 함수와 함께 사용하여 그룹별 요약 정보 제공
- 주요 집계 함수
  - COUNT(): 행의 개수 반환, 컬럼 이름을 쓰면 NULL을 안세고, *나 1을 쓰면 NULL을 센다.
  - SUM(): 합계 반환
  - AVG(): 평균 반환
  - MAX(): 최대값 반환
  - MIN(): 최소값 반환
- WHERE 절은 GROUP BY 전에 조건을 필터링
```
-- 5,000 이상 급여를 가진 직원만 그룹핑
SELECT DEPARTMENT_ID, COUNT(*) AS EMP_COUNT
FROM EMPLOYEES
WHERE SALARY >= 5000
GROUP BY DEPARTMENT_ID;
```
- HAVING 절은 GROUP BY 후에 그룹별 조건 필터링
```
-- 평균 급여가 6,000 이상인 부서만 출력
SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
HAVING AVG(SALARY) >= 6000;
```
