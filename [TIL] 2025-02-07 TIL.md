# TIL: 주요 class
## 학습 날짜: 2025년 2월 7일
## 오늘 내가 새롭게 배운 것들
**String**
- 자바는 기본 데이터 타입에 문자열이 없고 클래스로 제공
- 객체가 생성되는 heap 영역에 String Pool 이 존재
- "" 를 이용해서 문자열 객체를 생성하면 String Pool에 저장
- new keyword와 생성자를 이용해서 문자열 객체를 생성하면 일반 heap 영역에 저장
- String은 immutable(불변) 특성: String의 값을 수정하면 새로운 String을 생성, 같은 값이면 주소를 공유
  ![image](https://github.com/user-attachments/assets/b3554939-6b70-4c14-a4c5-633fcbf886e2)

**Array**
- 같은 type의 data를 여러 개 저장할 수 있는 자료구조
- '[]'를 이용해서 사용
- 배열은 객체이다
- new keyword를 사용
- 크기가 고정

**List**
- 크기가 유동적
- 각기 다른 데이터 타입을 저장 가능 (일반적으로는 generic을 사용해서 같은 data type만 저장하도록 제한)
- 객체만 저장 가능(기본형 타입은 저장 불가)
- 대표적으로 ArrayList 사용 -> 배열과 유사한 특성을 가진 List

**Map**
- (key, value) 쌍으로 이루어진 데이터를 저장할 수 있는 집합형 자료구조
- key, value 모두 객체만 저장 가능
- 일반적으로는 순서 개념이 없음
- 주로 HashMap 사용

**Set**
- 데이터의 중복 제거
- 순서 개념이 없음
- 주로 HashSet 사용
- Iterator(반복자)와 while문을 이용하여 Set에 있는 모든 데이터를 출력 가능

**Exception**
- 특정 상황이 발생하면 이 클래스의 인스턴스를 JVM이 생성
- 처리되지 않으면 프로그램이 비정상 종료됨
- Exception handling을 통해 프로그램이 비정상 종료가 되는 걸 방지
- Exception handling의 방법은 try~catch 구문이 유일
