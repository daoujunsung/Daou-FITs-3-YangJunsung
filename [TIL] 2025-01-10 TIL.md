# TIL: 컴퓨터 통신과 네트워킹
## 학습 날짜: 2025년 1월 10일
## 오늘 내가 새롭게 배운 것들
**컴퓨터 통신**
- 데이터 통신(Data Communication): 컴퓨터나 기기가 데이터를 주고 받는 통신
- 컴퓨터 네트워크(Computer Network): 컴퓨터 여러 대가 데이터를 주고 받는 통신 시스템
- 인터넷(Internet): 네트워크를 하나로 묶는 기술. 전 세계의 수많은 컴퓨터를 연결한 네트워크 집합체

**컴퓨터 네트워크의 종류**
- 근거리 네트워크(LAN, Local Area Network): 수 미터에서 1킬로미터 내의 컴퓨터를 연결
- 장거리 네트워크(WAN, Wide Area Network): 국가 전체를 연결하거나 국가 간을 연결
- 중거리 네트워크(MAN, Metropolitan Area Network): 대도시 간을 연결
- 개인 네트워크(PAN, Personal Area Network): 가까운 주변 장치들을 연결
- 초근거리 네트워크(NFC, Near Field Communication): 수 센티미터 거리에서 통신

**LAN 토폴로지**
- 컴퓨터를 연결하는 LAN의 구조, 위상 배치에 따라 3가지로 나눔
- 링형
  - 장치들을 원형으로 연결
  - 하나의 장치에 문제가 생기면 전체가 작동을 하지 못할 위험이 있음
  - 데이터 전송을 위해 토큰(Token) 사용
- 버스형
  - 하나의 통신 회선에 장치를 연결
  - 메인 회선에 문제가 생기면 전체가 셧다운이 되는 위험이 있음
  - 데이터를 목적지 주소와 함께 버스에 실어 보냄
- 스타형
  - 중앙에 위치한 한 장치에서 모든 장치를 연결
  - 하나의 장치에서 문제가 생겨도 다른 장치에 문제가 없지만, 중앙에 위치한 장치에 문제가 생기면 모두 멈추게 됨
  - 항상 중앙 장치를 통해 데이터 전송이 이루어짐

**가상 사설망(VPN, Virtual Private Network)**
- 인터넷과 같은 공용 네트워크를 통해 사설 네트워크에 안전하게 연결할 수 있도록 하는 기술
- 사용자가 인터넷을 통해 원격으로 연결된 네트워크에 안전하게 접근
- 데이터가 암호화되어 전송되기 때문에 정보 유출이나 해킹으로부터 보호

**통신의 분류**
- 통신 방향에 따른 분류
  - 단방향 통신: 한쪽으로만 통신 가능 ex) 모스 부호, 방송
  - 양방향 통신: 양쪽으로 동시에 통신 가능 ex) 대부분의 통신 시스템
  - 반양방향 통신: 양방향 통신이지만 어느 한순간에는 단방향 ex) 무전기
 
- 통신 대상에 따른 분류
  - 유니캐스트: 일대일 연결 ex) 전화
  - 브로드캐스트: 보내는 기기 한 대와 불특정 다수를 연결 ex) 방송
  - 멀티캐스트: 보내는 기기 한 대와 받는 기기 여러 대를 연결 ex) 집단 화상회의
 
**네트워크 통신의 계층 모델**
- 물리 계층: LAN에서 통신기기의 연결을 담당
- 데이터 링크 계층: 인접한 기기 간에 오류 없이 데이터를 전송하도록 제어
- 네트워크 계층: 여러 LAN을 하나로 묶어서 인터넷을 만드는 핵심 계층
- 전송 계층: 네트워크 계층에서 목적지까지 오면서 생기는 오류를 보정
- 응용 계층: 인터넷을 사용하는 각종 응용 프로그램이 위치하는 계층
- 계층의 독립성: 각 계층은 자신보다 상위 계층의 작업을 알지 못하고 각 계층은 같은 계층끼리만 통신

**OSI 7계층 모델**
- 국제 표준화 기구에서 개발한 네트워크 모델
- 5계층 모델에서 세션 계층, 표현 계층이 추가

**TCP/IP 모델**
- 현재 가장 많이 사용되는 네트워크 계층의 모델
- 로컬 네트워크: 물리 계층 + 데이터 링크 계층
- IP(Internet Protocol): 네트워크 계층
- TCP(Transmission Control Protocol): 전송 계층
- HTTP(HyperText Transfer Protocol), FTP(File Transfer Protocol): 응용 계층

**헤더(Header)**
- 데이터 전송을 위해 추가되는 자료구조
- 패킷(Packet): 네트워크 계층에서 다루는 데이터 단위

**프로토콜(Protocol)**
- 네트워크 통신에서 데이터를 주고 받을 때의 규칙과 절차를 정의한 것
- 컴퓨터나 네트워크 장치들이 서로 정보를 교환할 때, 프로토콜을 통해 서로의 요구 사항과 동작을 이해하고 일관되게 처리'

**TCP/IP**
- 인터넷의 기초 프로토콜
- 네트워크로 연결된 컴퓨터 간에 메시지를 안정적으로 전달하는 데 사용
- IP(Internet Protocol): 네트워크 사이에서 데이터를 주고 받는 규칙을 정한 프로토콜
- TCP(Transmission Control Protocol): 데이터를 전송할 때 생기는 오류를 바로 잡고, 데이터를 목적지 프로그램에 전달

**물리 계층**
- 실제 전송 매체에서 데이터를 전기적 신호나 광신호로 전송하는 프로토콜
- 전송 매체: 신호를 한쪽에서 다른 쪽으로 전달하는데 사용되는 물질, 유선 = 통신선, 무선 = 공기
- 통신선(Communication Line): 선이 길어질수록 신호의 세기가 감소하고 외부의 힘에 영향을 받음
  - 꼬임선(Twisted Pair): 두 개의 전선을 꼬아 외부의 영향을 덜 받게 함
  - 동축선(Coaxial Cable): 중앙에 구리선이 있고 이를 내부 피복으로 감싼 구조
  - 광섬유(Optical Fiber): 빛을 이용하여 데이터를 전송, 도청이 어려움
 
**데이터 링크 계층**
- 같은 네트워크 내에서 패킷을 물리적 주소로 전송
  - 프레임(Frame): 데이터 링크 헤더 + 패킷
- 전송 방식
  - 이더넷(Ethernet): 유선 LAN에서 널리 사용되는 데이터 링크 프로토콜, MAC 주소를 사용
    - MAC 주소: 48비트 길이로 12자리의 16진수로 표현, 첫 6자리는 제조사 식별자, 나머지 6자리는 각 장치에 고유한 식별 번호      
      ex) 00:1A:2B:3C:4D:5E
  - Wi-Fi(IEEE 802.11): 무선 네트워크에서 사용되는 프로토콜
- 오류를 처리하는 방식
  - 데이터 중복 전송: 송신측에서 수신측으로 각 데이터를 두 번씩 보내는 방법
  - 중복 정보 전송: 전송할 데이터에 대한 중복 정보(예를 들어 패리티 비트)를 데이터와 함께 보내는 방법
- 오류 처리 코드
  - 오류 탐색 코드: 패리티 비트, 체크섬, CRC 코드, 오류를 찾기만 함
  - 오류 보정 코드: 허밍 코드, 오류를 찾고 보정, 오버헤드가 큼
- 패리티 비트(Parity Bit)
  - 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식
  - 오류가 짝수 개 일어나면 발견할 수 없음, 네트워크에서 발생한 오류는 연속 오류일 가능성이 높아 치명적
- 수직 중복 검사(VRC, Vertical Redundancy Check): 데이터에 대한 패리티 비트를 구해 데이터와 함께 수신 측으로 전송
- 세로 중복 검사(LRC, Longitudinal Redundancy Check): 데이터를 행렬의 형태로 구성해 오류 검출
- 순환 중복 검사(CRC, Cyclic Redundancy Check): 숫자를 추가한 데이터를 CRC 코드 값으로 나누었을 때 0이 되도록 함, 가장 널리 사
- 체크섬(Checksum): 보내려는 데이터를 일정 크기로 자르고 이를 모두 더하여 코드를 만듦- 통신 대상에 따른 분류
  - 유니캐스트: 일대일 연결 ex) 전화
  - 브로드캐스트: 보내는 기기 한 대와 불특정 다수를 연결 ex) 방송
  - 멀티캐스트: 보내는 기기 한 대와 받는 기기 여러 대를 연결 ex) 집단 화상회의
 
**네트워크 통신의 계층 모델**
- 물리 계층: LAN에서 통신기기의 연결을 담당
- 데이터 링크 계층: 인접한 기기 간에 오류 없이 데이터를 전송하도록 제어
- 네트워크 계층: 여러 LAN을 하나로 묶어서 인터넷을 만드는 핵심 계층
- 전송 계층: 네트워크 계층에서 목적지까지 오면서 생기는 오류를 보정
- 응용 계층: 인터넷을 사용하는 각종 응용 프로그램이 위치하는 계층
- 계층의 독립성: 각 계층은 자신보다 상위 계층의 작업을 알지 못하고 각 계층은 같은 계층끼리만 통신

**OSI 7계층 모델**
- 국제 표준화 기구에서 개발한 네트워크 모델
- 5계층 모델에서 세션 계층, 표현 계층이 추가

**TCP/IP 모델**
- 현재 가장 많이 사용되는 네트워크 계층의 모델
- 로컬 네트워크: 물리 계층 + 데이터 링크 계층
- IP(Internet Protocol): 네트워크 계층
- TCP(Transmission Control Protocol): 전송 계층
- HTTP(HyperText Transfer Protocol), FTP(File Transfer Protocol): 응용 계층

**헤더(Header)**
- 데이터 전송을 위해 추가되는 자료구조
- 패킷(Packet): 네트워크 계층에서 다루는 데이터 단위

**프로토콜(Protocol)**
- 네트워크 통신에서 데이터를 주고 받을 때의 규칙과 절차를 정의한 것
- 컴퓨터나 네트워크 장치들이 서로 정보를 교환할 때, 프로토콜을 통해 서로의 요구 사항과 동작을 이해하고 일관되게 처리

**TCP/IP**
- 인터넷의 기초 프로토콜
- 네트워크로 연결된 컴퓨터 간에 메시지를 안정적으로 전달하는 데 사용
- IP(Internet Protocol): 네트워크 사이에서 데이터를 주고 받는 규칙을 정한 프로토콜
- TCP(Transmission Control Protocol): 데이터를 전송할 때 생기는 오류를 바로 잡고, 데이터를 목적지 프로그램에 전달

**물리 계층**
- 실제 전송 매체에서 데이터를 전기적 신호나 광신호로 전송하는 프로토콜
- 전송 매체: 신호를 한쪽에서 다른 쪽으로 전달하는데 사용되는 물질, 유선 = 통신선, 무선 = 공기
- 통신선(Communication Line): 선이 길어질수록 신호의 세기가 감소하고 외부의 힘에 영향을 받음
  - 꼬임선(Twisted Pair): 두 개의 전선을 꼬아 외부의 영향을 덜 받게 함
  - 동축선(Coaxial Cable): 중앙에 구리선이 있고 이를 내부 피복으로 감싼 구조
  - 광섬유(Optical Fiber): 빛을 이용하여 데이터를 전송, 도청이 어려움
 
**데이터 링크 계층**
- 같은 네트워크 내에서 패킷을 물리적 주소로 전송
  - 프레임(Frame): 데이터 링크 헤더 + 패킷
- 전송 방식
  - 이더넷(Ethernet): 유선 LAN에서 널리 사용되는 데이터 링크 프로토콜, MAC 주소를 사용
    - MAC 주소: 48비트 길이로 12자리의 16진수로 표현, 첫 6자리는 제조사 식별자, 나머지 6자리는 각 장치에 고유한 식별 번호      
      ex) 00:1A:2B:3C:4D:5E
  - Wi-Fi(IEEE 802.11): 무선 네트워크에서 사용되는 프로토콜
- 오류를 처리하는 방식
  - 데이터 중복 전송: 송신측에서 수신측으로 각 데이터를 두 번씩 보내는 방법
  - 중복 정보 전송: 전송할 데이터에 대한 중복 정보(예를 들어 패리티 비트)를 데이터와 함께 보내는 방법
- 오류 처리 코드
  - 오류 탐색 코드: 패리티 비트, 체크섬, CRC 코드, 오류를 찾기만 함
  - 오류 보정 코드: 허밍 코드, 오류를 찾고 보정, 오버헤드가 큼
- 패리티 비트(Parity Bit)
  - 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식
  - 오류가 짝수 개 일어나면 발견할 수 없음, 네트워크에서 발생한 오류는 연속 오류일 가능성이 높아 치명적
- 수직 중복 검사(VRC, Vertical Redundancy Check): 데이터에 대한 패리티 비트를 구해 데이터와 함께 수신 측으로 전송
- 세로 중복 검사(LRC, Longitudinal Redundancy Check): 데이터를 행렬의 형태로 구성해 오류 검출
- 순환 중복 검사(CRC, Cyclic Redundancy Check): 숫자를 추가한 데이터를 CRC 코드 값으로 나누었을 때 0이 되도록 함, 가장 널리 사
- 체크섬(Checksum): 보내려는 데이터를 일정 크기로 자르고 이를 모두 더하여 코드를 만듦

**네트워크 계층**
- 데이터를 목적지까지 라우팅하고 전달하는 역할
  - 라우팅(Routing): 어떤 길로 패킷을 보낼지 결정
  - 라우터(Router): 패킷을 어느 쪽 네트워크로 보낼지 결정하는 통신 장비
- IP 주소
  - 인터넷에 연결된 여러 장치를 식별하는 번호
  - 네 개의 10진수로 구성, 각각의 10진수는 여덟 자리 2진수를 의미
- IP 주소의 구분
  - 네트워크 주소: 해당 장치가 속해 있는 네트워크를 식별
  - 호스트 주소: 해당 네트워크 내에서 해당 장치를 식별
- 클래스 : 주어진 IP주소의 첫번째 자리로, A(0~127), B(128~191), C(192~223), D, E의 다섯 가지 클래스로 구분, D, E는 특수한 목적
- 서브넷 마스크(Subnet Mask)
  - IP 네트워크에서 서브넷을 나누기 위해 사용
  - 네트워크 주소: 서브넷 마스크에서 1로 설정, 255
  - 호스트 주소: 서브넷 마스크에서 0으로 설정, 0
- IPv4(Internet Protocol version 4)
  - 32비트 주소 체계 사용
  - 0.0.0.0 ~ 255.255.255.255
  - 기기가 움직이면 지역 코드가 변경되어 IP 주소를 수시로 변경해야 하는 등 주소 부족 문제가 발생할 수 있음
- IPv6(Internet Protocol version 6)
  - 128비트 주소 체계 사용
  - 훨씬 더 많은 고유 주소를 제공하여 주소 부족 문제를 해결할 수 있음
- IP 헤더
  - 헤더의 크기: 20바이트
  - 헤더의 필드 구성: 버전, 헤더 길이, 서비스 타입, 전체 길이, ID, 조각 상태, 조각의 위치, TTL, 프로토콜, 체크섬, 송신측 IP 주소, 수신측 IP 주소, 옵션
- MAC 헤더
  - ARP(Address Resolution Protocol): 수신측의 MAC 주소를 구할 때 사용하는 프로토콜
  - 헤더의 크기: 14바이트
  - 헤더의 필드 구성: 송신측 MAC 주소, 수신측 MAC 주소, 이더넷 타입
- 라우팅 테이블: 데이터를 어디로 보낼 지 나타낸 표, 수신 네트워크 주소를 비교하고 그에 맞는 게이트웨이로 데이터 전송, 없으면 첫 행으로 적용
- 게이트웨이(Gateway): 컴퓨터 네트워크에서 다른 네트워크와 연결되도록 도와주는 장치로 네트워크 간의 다리 역할을 함
- 허브(Hub)
  - 컴퓨터 네트워크에서 여러 장치를 연결하는 데 사용되는 네트워크 장비로 단순한 재전송 장치
  - 주로 소규모 네트워크나 매우 기본적인 용도로 사용
- 스위치(Switch)
  - 네트워크 장치들 간에 데이터 프레임을 전성하는 지능형 장비
  - 데이터를 필요한 장치로만 전송
  - 네트워크 트래픽을 효율적으로 관리
  - 대규모 네트워크와 고급 네트워크 환경에 적합
- 동적 라우팅 프로토콜
  - RIP(Routing Information Protocol)
    - 거리 벡터(Distance Vector) 기반의 라우팅 프로토콜
    - 목적지까지의 거리를 홉 수(Hop Count)로 측정, 최대 홉 수를 15로 제한   
  - OSPF(Open Shortest Path First)
    - 링크 상태(Link State) 라우팅 프로토콜로 계층적 라우팅을 지원
    - 네트워크의 각 라우터가 자신의 상태 정보를 전체 네트워크에 전파하여 최적의 경로를 계산
  - BGP(Border Gateway Protocol)
    - 경계 라우팅 프로토콜로 인터넷의 핵심 라우팅 프로토콜
    - 서로 다른 Autonomous System(AS) 간에 라우팅 정보를 교환
    - 경로 벡터(Path Vector)를 이용하여 네트워크 간의 데이터 경로를 결정 오류 처리 코드
  - 오류 탐색 코드: 패리티 비트, 체크섬, CRC 코드, 오류를 찾기만 함
  - 오류 보정 코드: 허밍 코드, 오류를 찾고 보정, 오버헤드가 큼
- 패리티 비트(Parity Bit)
  - 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식
  - 오류가 짝수 개 일어나면 발견할 수 없음, 네트워크에서 발생한 오류는 연속 오류일 가능성이 높아 치명적
- 수직 중복 검사(VRC, Vertical Redundancy Check): 데이터에 대한 패리티 비트를 구해 데이터와 함께 수신 측으로 전송
- 세로 중복 검사(LRC, Longitudinal Redundancy Check): 데이터를 행렬의 형태로 구성해 오류 검출
- 순환 중복 검사(CRC, Cyclic Redundancy Check): 숫자를 추가한 데이터를 CRC 코드 값으로 나누었을 때 0이 되도록 함, 가장 널리 사
- 체크섬(Checksum): 보내려는 데이터를 일정 크기로 자르고 이를 모두 더하여 코드를 만듦

**네트워크 계층**
- 데이터를 목적지까지 라우팅하고 전달하는 역할
  - 라우팅(Routing): 어떤 길로 패킷을 보낼지 결정
  - 라우터(Router): 패킷을 어느 쪽 네트워크로 보낼지 결정하는 통신 장비
- IP 주소
  - 인터넷에 연결된 여러 장치를 식별하는 번호
  - 네 개의 10진수로 구성, 각각의 10진수는 여덟 자리 2진수를 의미
- IP 주소의 구분
  - 네트워크 주소: 해당 장치가 속해 있는 네트워크를 식별
  - 호스트 주소: 해당 네트워크 내에서 해당 장치를 식별
- 클래스 : 주어진 IP주소의 첫번째 자리로, A(0~127), B(128~191), C(192~223), D, E의 다섯 가지 클래스로 구분, D, E는 특수한 목적
- 서브넷 마스크(Subnet Mask)
  - IP 네트워크에서 서브넷을 나누기 위해 사용
  - 네트워크 주소: 서브넷 마스크에서 1로 설정, 255
  - 호스트 주소: 서브넷 마스크에서 0으로 설정, 0
- IPv4(Internet Protocol version 4)
  - 32비트 주소 체계 사용
  - 0.0.0.0 ~ 255.255.255.255
  - 기기가 움직이면 지역 코드가 변경되어 IP 주소를 수시로 변경해야 하는 등 주소 부족 문제가 발생할 수 있음
- IPv6(Internet Protocol version 6)
  - 128비트 주소 체계 사용
  - 훨씬 더 많은 고유 주소를 제공하여 주소 부족 문제를 해결할 수 있음
- IP 헤더
  - 헤더의 크기: 20바이트
  - 헤더의 필드 구성: 버전, 헤더 길이, 서비스 타입, 전체 길이, ID, 조각 상태, 조각의 위치, TTL, 프로토콜, 체크섬, 송신측 IP 주소, 수신측 IP 주소, 옵션
- MAC 헤더
  - ARP(Address Resolution Protocol): 수신측의 MAC 주소를 구할 때 사용하는 프로토콜
  - 헤더의 크기: 14바이트
  - 헤더의 필드 구성: 송신측 MAC 주소, 수신측 MAC 주소, 이더넷 타입
- 라우팅 테이블: 데이터를 어디로 보낼 지 나타낸 표, 수신 네트워크 주소를 비교하고 그에 맞는 게이트웨이로 데이터 전송, 없으면 첫 행으로 적용
- 게이트웨이(Gateway): 컴퓨터 네트워크에서 다른 네트워크와 연결되도록 도와주는 장치로 네트워크 간의 다리 역할을 함
- 허브(Hub)
  - 컴퓨터 네트워크에서 여러 장치를 연결하는 데 사용되는 네트워크 장비로 단순한 재전송 장치
  - 주로 소규모 네트워크나 매우 기본적인 용도로 사용
- 스위치(Switch)
  - 네트워크 장치들 간에 데이터 프레임을 전성하는 지능형 장비
  - 데이터를 필요한 장치로만 전송
  - 네트워크 트래픽을 효율적으로 관리
  - 대규모 네트워크와 고급 네트워크 환경에 적합
- 동적 라우팅 프로토콜
  - RIP(Routing Information Protocol)
    - 거리 벡터(Distance Vector) 기반의 라우팅 프로토콜
    - 목적지까지의 거리를 홉 수(Hop Count)로 측정, 최대 홉 수를 15로 제한   
  - OSPF(Open Shortest Path First)
    - 링크 상태(Link State) 라우팅 프로토콜로 계층적 라우팅을 지원
    - 네트워크의 각 라우터가 자신의 상태 정보를 전체 네트워크에 전파하여 최적의 경로를 계산
  - BGP(Border Gateway Protocol)
    - 경계 라우팅 프로토콜로 인터넷의 핵심 라우팅 프로토콜
    - 서로 다른 Autonomous System(AS) 간에 라우팅 정보를 교환
    - 경로 벡터(Path Vector)를 이용하여 네트워크 간의 데이터 경로를 결정
- IP 주소 유형
  - 사설 IP 주소(Private IP Address)
    - 내부 네트워크에서 사용되며, 공인 IP 주소와 겹칠 수 없음
    - 사설 IP 주소는 외부 인터넷과 직접 통신할 수 없음
    - NAT(Network Address Translation) 기술을 통해 공인 IP로 변환되어 인터넷과 연결
  - 공인 IP 주소(Public IP Address)
     - 인터넷에서 고유한 주소로, 전 세계적으로 유일
     - ISP(인터넷 서비스 제공업체)가 할당
- DHCP(Dynamic Host Configuration Protocol)
  - 네트워크에서 장치들이 IP 주소 및 기타 필요한 네트워크 설정을 자동으로 할당받을 수 있도록 도와주는 프로토콜
  - 초고속 인터넷 서비스 회사에서 개인의 서버 설치를 막기 위해 IP 주소를 자동으로 할당 오류 처리 코드
  - 오류 탐색 코드: 패리티 비트, 체크섬, CRC 코드, 오류를 찾기만 함
  - 오류 보정 코드: 허밍 코드, 오류를 찾고 보정, 오버헤드가 큼
- 패리티 비트(Parity Bit)
  - 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식
  - 오류가 짝수 개 일어나면 발견할 수 없음, 네트워크에서 발생한 오류는 연속 오류일 가능성이 높아 치명적
- 수직 중복 검사(VRC, Vertical Redundancy Check): 데이터에 대한 패리티 비트를 구해 데이터와 함께 수신 측으로 전송
- 세로 중복 검사(LRC, Longitudinal Redundancy Check): 데이터를 행렬의 형태로 구성해 오류 검출
- 순환 중복 검사(CRC, Cyclic Redundancy Check): 숫자를 추가한 데이터를 CRC 코드 값으로 나누었을 때 0이 되도록 함, 가장 널리 사
- 체크섬(Checksum): 보내려는 데이터를 일정 크기로 자르고 이를 모두 더하여 코드를 만듦

**네트워크 계층**
- 데이터를 목적지까지 라우팅하고 전달하는 역할
  - 라우팅(Routing): 어떤 길로 패킷을 보낼지 결정
  - 라우터(Router): 패킷을 어느 쪽 네트워크로 보낼지 결정하는 통신 장비
- IP 주소
  - 인터넷에 연결된 여러 장치를 식별하는 번호
  - 네 개의 10진수로 구성, 각각의 10진수는 여덟 자리 2진수를 의미
- IP 주소의 구분
  - 네트워크 주소: 해당 장치가 속해 있는 네트워크를 식별
  - 호스트 주소: 해당 네트워크 내에서 해당 장치를 식별
- 클래스 : 주어진 IP주소의 첫번째 자리로, A(0~127), B(128~191), C(192~223), D, E의 다섯 가지 클래스로 구분, D, E는 특수한 목적
- 서브넷 마스크(Subnet Mask)
  - IP 네트워크에서 서브넷을 나누기 위해 사용
  - 네트워크 주소: 서브넷 마스크에서 1로 설정, 255
  - 호스트 주소: 서브넷 마스크에서 0으로 설정, 0
- IPv4(Internet Protocol version 4)
  - 32비트 주소 체계 사용
  - 0.0.0.0 ~ 255.255.255.255
  - 기기가 움직이면 지역 코드가 변경되어 IP 주소를 수시로 변경해야 하는 등 주소 부족 문제가 발생할 수 있음
- IPv6(Internet Protocol version 6)
  - 128비트 주소 체계 사용
  - 훨씬 더 많은 고유 주소를 제공하여 주소 부족 문제를 해결할 수 있음
- IP 헤더
  - 헤더의 크기: 20바이트
  - 헤더의 필드 구성: 버전, 헤더 길이, 서비스 타입, 전체 길이, ID, 조각 상태, 조각의 위치, TTL, 프로토콜, 체크섬, 송신측 IP 주소, 수신측 IP 주소, 옵션
- MAC 헤더
  - ARP(Address Resolution Protocol): 수신측의 MAC 주소를 구할 때 사용하는 프로토콜
  - 헤더의 크기: 14바이트
  - 헤더의 필드 구성: 송신측 MAC 주소, 수신측 MAC 주소, 이더넷 타입
- 라우팅 테이블: 데이터를 어디로 보낼 지 나타낸 표, 수신 네트워크 주소를 비교하고 그에 맞는 게이트웨이로 데이터 전송, 없으면 첫 행으로 적용
- 게이트웨이(Gateway): 컴퓨터 네트워크에서 다른 네트워크와 연결되도록 도와주는 장치로 네트워크 간의 다리 역할을 함
- 허브(Hub)
  - 컴퓨터 네트워크에서 여러 장치를 연결하는 데 사용되는 네트워크 장비로 단순한 재전송 장치
  - 주로 소규모 네트워크나 매우 기본적인 용도로 사용
- 스위치(Switch)
  - 네트워크 장치들 간에 데이터 프레임을 전성하는 지능형 장비
  - 데이터를 필요한 장치로만 전송
  - 네트워크 트래픽을 효율적으로 관리
  - 대규모 네트워크와 고급 네트워크 환경에 적합
- 동적 라우팅 프로토콜
  - RIP(Routing Information Protocol)
    - 거리 벡터(Distance Vector) 기반의 라우팅 프로토콜
    - 목적지까지의 거리를 홉 수(Hop Count)로 측정, 최대 홉 수를 15로 제한   
  - OSPF(Open Shortest Path First)
    - 링크 상태(Link State) 라우팅 프로토콜로 계층적 라우팅을 지원
    - 네트워크의 각 라우터가 자신의 상태 정보를 전체 네트워크에 전파하여 최적의 경로를 계산
  - BGP(Border Gateway Protocol)
    - 경계 라우팅 프로토콜로 인터넷의 핵심 라우팅 프로토콜
    - 서로 다른 Autonomous System(AS) 간에 라우팅 정보를 교환
    - 경로 벡터(Path Vector)를 이용하여 네트워크 간의 데이터 경로를 결정 오류 처리 코드
  - 오류 탐색 코드: 패리티 비트, 체크섬, CRC 코드, 오류를 찾기만 함
  - 오류 보정 코드: 허밍 코드, 오류를 찾고 보정, 오버헤드가 큼
- 패리티 비트(Parity Bit)
  - 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식
  - 오류가 짝수 개 일어나면 발견할 수 없음, 네트워크에서 발생한 오류는 연속 오류일 가능성이 높아 치명적
- 수직 중복 검사(VRC, Vertical Redundancy Check): 데이터에 대한 패리티 비트를 구해 데이터와 함께 수신 측으로 전송
- 세로 중복 검사(LRC, Longitudinal Redundancy Check): 데이터를 행렬의 형태로 구성해 오류 검출
- 순환 중복 검사(CRC, Cyclic Redundancy Check): 숫자를 추가한 데이터를 CRC 코드 값으로 나누었을 때 0이 되도록 함, 가장 널리 사
- 체크섬(Checksum): 보내려는 데이터를 일정 크기로 자르고 이를 모두 더하여 코드를 만듦

**네트워크 계층**
- 데이터를 목적지까지 라우팅하고 전달하는 역할
  - 라우팅(Routing): 어떤 길로 패킷을 보낼지 결정
  - 라우터(Router): 패킷을 어느 쪽 네트워크로 보낼지 결정하는 통신 장비
- IP 주소
  - 인터넷에 연결된 여러 장치를 식별하는 번호
  - 네 개의 10진수로 구성, 각각의 10진수는 여덟 자리 2진수를 의미
- IP 주소의 구분
  - 네트워크 주소: 해당 장치가 속해 있는 네트워크를 식별
  - 호스트 주소: 해당 네트워크 내에서 해당 장치를 식별
- 클래스 : 주어진 IP주소의 첫번째 자리로, A(0~127), B(128~191), C(192~223), D, E의 다섯 가지 클래스로 구분, D, E는 특수한 목적
- 서브넷 마스크(Subnet Mask)
  - IP 네트워크에서 서브넷을 나누기 위해 사용
  - 네트워크 주소: 서브넷 마스크에서 1로 설정, 255
  - 호스트 주소: 서브넷 마스크에서 0으로 설정, 0
- IPv4(Internet Protocol version 4)
  - 32비트 주소 체계 사용
  - 0.0.0.0 ~ 255.255.255.255
  - 기기가 움직이면 지역 코드가 변경되어 IP 주소를 수시로 변경해야 하는 등 주소 부족 문제가 발생할 수 있음
- IPv6(Internet Protocol version 6)
  - 128비트 주소 체계 사용
  - 훨씬 더 많은 고유 주소를 제공하여 주소 부족 문제를 해결할 수 있음
- IP 헤더
  - 헤더의 크기: 20바이트
  - 헤더의 필드 구성: 버전, 헤더 길이, 서비스 타입, 전체 길이, ID, 조각 상태, 조각의 위치, TTL, 프로토콜, 체크섬, 송신측 IP 주소, 수신측 IP 주소, 옵션
- MAC 헤더
  - ARP(Address Resolution Protocol): 수신측의 MAC 주소를 구할 때 사용하는 프로토콜
  - 헤더의 크기: 14바이트
  - 헤더의 필드 구성: 송신측 MAC 주소, 수신측 MAC 주소, 이더넷 타입
- 라우팅 테이블: 데이터를 어디로 보낼 지 나타낸 표, 수신 네트워크 주소를 비교하고 그에 맞는 게이트웨이로 데이터 전송, 없으면 첫 행으로 적용
- 게이트웨이(Gateway): 컴퓨터 네트워크에서 다른 네트워크와 연결되도록 도와주는 장치로 네트워크 간의 다리 역할을 함
- 허브(Hub)
  - 컴퓨터 네트워크에서 여러 장치를 연결하는 데 사용되는 네트워크 장비로 단순한 재전송 장치
  - 주로 소규모 네트워크나 매우 기본적인 용도로 사용
- 스위치(Switch)
  - 네트워크 장치들 간에 데이터 프레임을 전성하는 지능형 장비
  - 데이터를 필요한 장치로만 전송
  - 네트워크 트래픽을 효율적으로 관리
  - 대규모 네트워크와 고급 네트워크 환경에 적합
- 동적 라우팅 프로토콜
  - RIP(Routing Information Protocol)
    - 거리 벡터(Distance Vector) 기반의 라우팅 프로토콜
    - 목적지까지의 거리를 홉 수(Hop Count)로 측정, 최대 홉 수를 15로 제한   
  - OSPF(Open Shortest Path First)
    - 링크 상태(Link State) 라우팅 프로토콜로 계층적 라우팅을 지원
    - 네트워크의 각 라우터가 자신의 상태 정보를 전체 네트워크에 전파하여 최적의 경로를 계산
  - BGP(Border Gateway Protocol)
    - 경계 라우팅 프로토콜로 인터넷의 핵심 라우팅 프로토콜
    - 서로 다른 Autonomous System(AS) 간에 라우팅 정보를 교환
    - 경로 벡터(Path Vector)를 이용하여 네트워크 간의 데이터 경로를 결정
- IP 주소 유형
  - 사설 IP 주소(Private IP Address)
    - 내부 네트워크에서 사용되며, 공인 IP 주소와 겹칠 수 없음
    - 사설 IP 주소는 외부 인터넷과 직접 통신할 수 없음
    - NAT(Network Address Translation) 기술을 통해 공인 IP로 변환되어 인터넷과 연결
  - 공인 IP 주소(Public IP Address)
     - 인터넷에서 고유한 주소로, 전 세계적으로 유일
     - ISP(인터넷 서비스 제공업체)가 할당
- DHCP(Dynamic Host Configuration Protocol)
  - 네트워크에서 장치들이 IP 주소 및 기타 필요한 네트워크 설정을 자동으로 할당받을 수 있도록 도와주는 프로토콜
  - 초고속 인터넷 서비스 회사에서 개인의 서버 설치를 막기 위해 IP 주소를 자동으로 할당
- 도메인 네임(Domain Name)
  - IP 주소에 이름을 부여한 것
  - 마침표(.)로 구분하여 계층화
  - 오른쪽에 있는 이름이 상위 계층
  - '이름.종류.국가' 순서
- 도메인 네임 시스템(DNS, Domain Name System): 도메인 네임과 IP 주소를 테이블 형태로 모아놓은 서버
