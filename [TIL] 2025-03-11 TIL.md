# TIL: 변수, 포인터
## 학습 날짜: 2025년 3월 11일
## 오늘 내가 새롭게 배운 것들
**변수**
- 지역변수(local variable), 자동변수(auto)
  - 중괄호 내부, 함수의 매개변수에서 사용되는 변수이다.
  - 지역변수의 지역은 함수의 내부, 중괄호 내부를 말한다
  - 만든 곳을 벗어나면 사라진다.
  - 변수의 메모리 공간이 소멸 된다.
  - 지역변수는 초기화 하지 않으면 컴파일 에러가 나거나 쓰레기값이 저장된다
- 전역변수(global)
  - 중괄호 외부에 선언되는 변수이다
  - 어느 지역에서든 참조할 수 있다.
  - 전처리기 밑에 선언하고 반드시 상수로 초기화 해야 한다.
  - 초기값을 지정하지 않으면 기본값이 0으로 자동 초기화 된다.
  - 모든 곳에 접근 가능하고 프로그램 종료 전에 메모리가 소멸되지 않는다
  - 잘못 이용하면 유지보수, 재사용등이 어려워 질 수 있다.
  - 이것의 단점을 보안한 것이 정적변수이다
- 정적변수(static)
  - 프로그램 종료되기 전까지 메모리가 소멸되지 않는 변수이다
  - 함수를 벗어나도 변수가 사리지지 않고 유지된다
  - 초기화할 때 반드시 상수로 초기화 해야 한다
  - 초기화하지 않으면 디폴트값 0으로 자동 초기화 된다
  - 종류
    - 정적 전역변수(static global variable)
      - 자신이 선언된 소스 파일에서만 사용할 수 있고 외부에서는 가져다 쓸 수 없다.
      - 전역변수에 static을 붙이면 변수의 범위를 파일 범위로 제한하는 효과를 낸다.
    - 정적 지역변수(static local variable)
      - 중괄호 내부에서만 사용할 수 있다.
      - 한번 초기화 되면 이후에 함수 호출 시 값의 초기화를 무시한다. 
- 레지스터 변수(register variable)
  - CPU 레지스터에 저장될 가능성이 있는 변수
  - 목적은 자주 사용하는 변수를 메모리가 아니라 레지스터에 저장하여 접근 속도를 높이는 것
  - 컴파일러가 register라고 쓰면 반드시 레지스터에 저장하는 것은 아니다.
  - 포인터로 접근할 수 없다(& 연산자 사용할 수 없다)
  - 현재 컴파일러는 최적화를 자동으로 수행하므로 register 키워드는 거의 사용하지 않는다
