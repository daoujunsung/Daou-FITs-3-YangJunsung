# TIL: 인덱스
## 학습 날짜: 2025년 1월 23일
## 오늘 내가 새롭게 배운 것들
**인덱스(Index)**
- 데이터베이스 테이블의 검색 성능을 향상시키기 위해 사용되는 특별한 데이터 구조
- 특정 데이터를 빠르게 찾아갈 수 있는 경로를 제공
- 사용 목적
  - 검색 성능 향상
  - 쿼리 실행 시간 단축
  - 테이블의 특정 컬럼을 기준으로 데이터 정렬 및 관리
- 동작 원리
  - 테이블 스캔(Table Full Scan)
    - 인덱스가 없는 경우 테이블의 모든 데이터를 처음부터 끝까지 읽어야 함
    - 데이터 양이 많아질수록 성능 저하 발생
  - 인덱스 스캔(Index Scan)
    - 인덱스를 사용하면 필요한 데이터가 저장된 위치를 빠르게 탐색 가능
    - 테이블의 일부만 읽기 때문에 성능 향상
- 구조
  - B-Tree 인덱스 (가장 일반적인 구조, Balanced-Tree Index)
    - 균형 이진 트리 형태로 데이터 검색에 최적화
    - 데이터가 정렬된 상태로 유지되며, 검색, 삽입, 삭제 시 다른 구조보다 효율적
    - 루트 노드 -> 중간 노드 -> 리프 노드 순서로 탐색
    - 리프 노드에는 실제 데이터 위치 정보 저장
- Sequential Access vs Random Access
  - Sequential Access
    - 높은 효율
    - 데이터를 순서대로 읽거나 쓰는 방식
    - 인덱스 Leaf Block을 읽는 방식
  - Random Access
    - 낮은 효율
    - 논리적, 물리적 순서와 무관
    - 한 건을 읽기 위해 한 Block씩 접근
- 장점
  - 검색 속도 향상: 데이터베이스에서 SELECT 쿼리를 실행할 때 효율적
  - 정렬된 데이터 관리: 인덱스를 사용하면 데이터가 자동으로 정렬
  - 대용량 데이터 처리: 테이블 크기가 커질수록 인덱스의 효율성이 증가
- 단점
  - 추가 저장 공간 필요: 인덱스는 테이블 외에도 별도의 공간을 차지
  - 쓰기 작업 성능 저하: INSERT, UPDATE, DELETE 작업 시 인덱스도 함께 갱신되어야 함
  - 인덱스 과다 생성 문제: 너무 많은 인덱스는 관리 비용 증가와 성능 저하를 초래
- 유형
  - 단일 컬럼 인덱스: 하나의 컬럼만 기준으로 인덱스를 생성
    ```
    CREATE INDEX IDX_CUSTOMER_NAME ON CUSTOMERS(NAME);
    ```
  - 결합 인덱스: 두 개 이상의 컬럼을 조합하여 생성
    ```
    CREATE INDEX IDX_LOAN_CUSTOMER_DATE ON LOANS(CUSTOMER_ID, LOAN_DATE);
    ```
  - 유니크 인덱스: 중복을 허용하지 않는 인덱스
    ```
    CREATE UNIQUE INDEX IDX_EMAIL ON CUSTOMERS(EMAIL);
    ```
- 인덱스 확인
  ```
  SELECT INDEX_NAME, TABLE_NAME, UNIQUENESS
  FROM USER_INDEXES
  WHERE TABLE_NAME = 'CUSTOMERS';
  ```
- 인덱스 삭제
  ```
  DROP INDEX IDX_CUSTOMER_NAME;
  ```
- 관리 팁
  - 자주 사용되는 컬럼에 생성: WHERE 조건, JOIN, ORDER BY, GROUP BY에 자주 등장하는 컬럼
  - 적절한 인덱스 개수 유지: 과도한 인덱스는 성능 저하를 초래
  - 쓰기와 읽기 비율 고려: 읽기 작업이 많은 테이블에 적합
  - 데이터 중복도 파악: 중복도가 낮은 컬럼은 B-Tree, 높은 컬럼은 Bitmap 인덱스 추천
- 설계 시 유의 사항
  - 인덱스 컬럼의 순서
    - 결합 인덱스에서 SELECT 조건에 자주 사용되는 컬럼을 우선 배치
    - 인덱스 스캔 범위 줄이기
    - 테이블 액세스 빈도 줄이기
    - 인덱스를 구성하는 선행 컬럼을 조건절에 사용
    - Equal 조건으로 자주 쓰이는 컬럼을 선행 컬럼으로 설정
  - 쓰기 작업 최적화: 데이터 변경 작업이 빈번한 테이블은 인덱스의 개수를 최소로 유지
  - 카디널리티(값의 고유도)가 높은 컬럼에 인덱스를 생성
  - 데이터 크기와 접근 빈도: 대용량 데이터 테이블에서 인덱스는 필수
- 인덱스를 타지 못한다는 것의 의미
  - 활용할 수 있는 인덱스가 있음에도 SQL 쿼리가 인덱스를 활용하지 않고 풀스캔하여 데이터를 조회하는 경우
  - 불필요한 데이터 검색으로 인해 성능 저하 발생
- 인덱스를 활용하지 못하게 하는 주요 원인
  - 인덱스 컬럼에 함수 또는 연산 사용
  - 데이터 타입 불일치
  - 와일드카드(%) 앞 부분에 사용
  - NULL 값 처리
  - OR 조건이 포함된 경우
- Reverse Key Index
  - 인덱스에 저장되는 키 값을 반전시켜 저장
  - 인덱스에 시퀀스나 날짜 등 순차적으로 증가하는 값이 입력될 때 특정 인덱스 block에 과도한 경합이 발생하는 것을 방지
  - Equal 검색만 가능
  - 다중 트랜잭션 환경에서 대량 삽입 작업이 발생하는 경우
  - 주로 읽기 작업이 많은 테이블에는 부적합
- Clustered Index
  - 테이블 데이터가 인덱스 순서에 따라 물리적으로 정렬되어 저장되는 인덱스
  - 각 테이블 당 하나의 클러스터형 인덱스만 생성 가능
  - 인덱스와 테이블이 같은 물리적 구조를 가지므로 검색 속도가 빠름 (범위 검색 성능 우수)
  - 한 테이블 당 하나만 가능
- IOT(Index Organized Table)
  - 인덱스 Leaf Block = 테이블
  - 테이블의 데이터를 B-Tree 인덱스에 저장
  - 인덱스 자체에 모든 데이터가 저장
  - 별도의 데이터와 인덱스를 유지하지 않으므로 저장 공간 절약
  - PK 검색이 매우 빠르지만 그 외의 값으로 검색하는 경우에는 느림
  - 삽입/삭제 성능 저하
  - 공통 코드 테이블 등 폭이 좁고 긴 테이블에 적합
