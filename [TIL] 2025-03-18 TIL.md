# TIL: 메모리 구조
## 학습 날짜: 2025년 3월 18일
## 오늘 내가 새롭게 배운 것들
**메모리 구조**
- 코드 영역(Text Segment): 프로그램의 실행 코드가 저장
  - 읽기 전용으로 코드가 실행되는 동안 수정 불가
  - 프로그램의 모든 함수와 명령어가 포함
  - 컴파일 시에 결정되며, 실행 중에는 변경 안됨
  - 메모리 보호를 위해 일반적으로 읽기 전용 속성을 가짐
- 전역변수 영역(Data Segment): 전역 변수와 정적 변수가 저장되는 영역
  - 프로그램 시작 시 초기화
  - 전역 변수는 프로그램 전체에서 접근 가능, 정적 변수는 선언된 함수 내에서만 접근 가능
  - 초기화된 전역 변수는 데이터 세그먼트의 초기화된 영역에, 초기화되지 않은 전역 변수는 BSS(Basic Storage Segment) 영역에 저장
- 스택 영역(Stack): 함수 호출 시 지역 변수와 매개변수, 반환 주소 등이 저장되는 영역
  - LIFO(Last In, First Out) 방식으로 작동, 가장 최근에 호출된 함수가 먼저 종료
  - 함수가 호출되면 스택 프레임이 생성되고, 함수가 종료되면 해당 스택 프레임이 해제
- 힙 영역(Heap): 동적으로 할당된 메모리가 저장되는 영역
  - malloc, calloc, realloc, free와 같은 함수를 통해 관리
  - 스택 영역의 크기는 제한적이며, 너무 많은 메모리를 사용하면 스택 오버플로우 발생
  - 메모리의 크기가 동적으로 결정되며, 프로그램 실행 중에 필요에 따라 할당하고 해제 가능
  - 힙 영역은 스택과 달리 크기가 제한적이지 않지만, 메모리 누수와 같은 문제가 발생할 수 있음
  - 프로그래머가 메모리 관리를 직접 해야하므로, 주의 필요
- C vs Java
  - 메모리 관리: C는 프로그래머가 직접 메모리를 관리하지만, Java는 가비지 컬렉터를 통해 자동으로 메모리 관리
  - 객체 저장: C는 객체의 동적 메모리 할당만을 위해 힙을 사용하지만, Java는 모든 객체가 힙에 저장
  - 코드 실행: Java는 JVM에서 실행되며, 바이트코드 형태로 저장

**malloc()**
- Memory Allication 함수는 C언어에서 동적 메모리를 할당하는 함수
- 실시간(런타임) 동안 힙 영역에 메모리를 할당
- 초기화되지 않은 메모리 블록의 포인터를 반환
- 할당된 메모리는 반드시 free()을 사용하여 해제
- 문제점
  - 초기화 되지 않은 메모리 블록은 쓰레기 값이 들어 간다,
  - 초기화가 필요하면 callock으로 사용하면 초기값이 자동으로 채워진다.

**calloc()**
- 힙에서 메모리 영역을 할당해주는 함수
- malloc에 비해서 매개변수가 하나 더 있음
- 메모리 할당과 0 초기화를 한번에 해결해주는 함수

**realloc()**
- 이미 할당된 메모리 영역을 재할당하기 위하여 사용
- 주로 기존에 할당된 메모리 영역의 크기를 변경할 경우 사용
- realloc 호출 후에는 기존 할당된 메모리 영역을 해제해서는 안됨
