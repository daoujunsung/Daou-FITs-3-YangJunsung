# TIL: 파일 입출력
## 학습 날짜: 2025년 3월 20일
## 오늘 내가 새롭게 배운 것들
**파일 입출력**
- Mode
  - "a": append, 파일이 없으면 빈 파일 생성, 파일 끝에 새로 추가
  - "w": write, 기존 파일 내용 삭제, 파일 없으면 빈 파일 생성
  - "r": read, 파일 없으면 오류
  - "+": 업데이트 지시자, 읽기와 쓰기 모두 쓸 때 사용
  - "t": 텍스트 파일 형태
  - "b": 이진 파일 형태
- 파일 종류
  - 텍스트 파일
    - 아스키 문자로 번역하여 저장
    - 메모장 파일
  - 이진 파일
    - 메모리 내용을 그대로 파일에 저장
    - 대용량 파일을 읽거나 쓰려면 이진 파일이 유리
- EOF
  - 파일 끝에 도달했을 때
  - 파일이 없을 때
  - 하드디스크 용량이 부족해서 새로운 파일을 만들 수 ㅓㅄ을 때
  - 동시에 열 수 있는 파일의 수를 넘어갈 때
- feof: 실제로 파일 끝에 도달했는지 확인하는 함수
- 파일 쓰기 함수
  - fputc(int ch, FILE* stream);
  - fputs(const char* str, FILE* stream);
  - fwrite(cont void \*buffer, size_t size, size_t count, FILE* stream);
- 파일 읽기 함수
  - int fgetc(FILE* stream): 1바이트를 파일에서 읽고 반환함
  - char* fgets(char* str, int count, FILE* stream): 텍스트 파일을 기준으로 파일 위치 표시자의 위치부터 한줄을 읽은 후 첫번째 매개변수가 가리키는 문자 배열에 저장
  - size_t fread(void \*buffer, size_t size, size_t count, FILE* stream): 파일 표시자의 위치부터 count 바이트만큼 읽은 후 첫번째 매개변수가 가리키는 메모리 영역에 복사
- 파일의 임의 접근: 파일의 임의의 위치에서 바로 읽기/쓰기를 할 수 있는 접근 방식
  - fseek(파일 포인터 변수명, 오프셋, 기준점)
    - 파일 포인터 변수에 연결된 파일 위치 지시자가 기준점으로부터 오프셋만큼 떨어진 곳을 가리키게 한다.
    - 즉, 다음에 읽기/쓰기를 시작할 위치를 (기준점 + 오프셋) 바이트 위치로 변경한다. 
    - 기준점(whence) : 오프셋을 적용할 기준점으로 다음 세 매크로 상수 중 한 개를 사용한다.
    - SEEK_SET 0이며 파일의 시작 지점을 의미
    - SEEK_CUR 1이며 파일의 현재 지점을 의미
    - SEEK_END 2이며 파일의 끝 지점을 의미
    - 오프셋(offset) : 기준점에서 몇 바이트 이동할 지를 나타내는 long형 정수로 기준점 이전이라면 음수를, 이후라면 양수를 사용한다.
    - 이동을 성공하면 0을 반환하고, 실패하면 0이 아닌 값을 반환한다.
  - rewind(파일 포인터 변수명 ): 파일 포인터에 연결된 파일의 다음 읽기/쓰기 위치를 파일의 시작 지점으로 이동한다. 
  - ftell( 파일 포인터 변수명 ): 파일 포인터 변수에 연결된 파일에서 다음 읽기/쓰기를 시작할 위치를 반환
