<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board.Like">
    <resultMap id="LikeResultMap" type="Like">
        <result property="boardId" column="board_id"/>
        <result property="memberId" column="member_id"/>
    </resultMap>

    <select id="countByMemberIdAndBoardId" parameterType="Like" resultType="int">
        <![CDATA[
        SELECT COUNT(*)
        FROM likes_tbl
        WHERE board_id = #{boardId}
        AND member_id = #{memberId}
        ]]>
    </select>

    <select id="countByBoardId" parameterType="int" resultType="int">
        <![CDATA[
        SELECT COUNT(*)
        FROM likes_tbl
        WHERE board_id = #{boardId}
        ]]>
    </select>

    <insert id="insertLike" parameterType="Like">
        <![CDATA[
        INSERT INTO likes_tbl (board_id, member_id)
        VALUES (#{boardId}, #{memberId})
        ]]>
    </insert>

    <delete id="deleteLike" parameterType="Like">
        <![CDATA[
        DELETE
        FROM likes_tbl
        WHERE board_id = #{boardId}
        AND member_id = #{memberId}
        ]]>
    </delete>
</mapper>