# TIL: 포인터
## 학습 날짜: 2025년 3월 12일
## 오늘 내가 새롭게 배운 것들
**포인터**
- '&' 연산자: 뒤에 오는 변수의 값이 저장된 메모리의 주소를 반환
- '*' 연산자: 뒤에 오는 포인터 변수가 가리키는 변수의 값을 변환
- 포인터 변수는 주소를 저장하는 변수
- 다중 포인터
  - 값을 가져오기 위해 메모리에 여러 번 접근해야 하므로 일반 변수를 사용하는 것보다 효율성은 떨어짐
  - 배열과 문자열의 조작에 포인터를 사용함으로써 간결하고 효율적으로 처리
  - 배열, 구조체, 공용체와 함수에 포인터를 사용하여 쉽게 접근
  - 배열의 첨자보다 포인터를 사용하는 것이 더 빠른 처리 가능
  - 동적 자료의 처리나 순환 자료 구조의 표현, 참조에 의한 호출 등에서 사용
  - 유지 보수가 어려워짐
- 배열과 포인터
  - 배열을 제어할 떄 배열의 첨자보다 포인터를 사용하는 것이 더 효율적
  - 배열은 모든 요소가 메모리의 연속된 기억 공간에 저장
  - 배열의 이름은 포인터 변수와 같은 역할
  - 배열의 이름은 배열 첫번째 요소의 주소 상수값을 가짐
  - 배열의 이름은 포인터 변수와 같은 역할을 하므로 포인터 변수 대신 직접 사용 가능
  - 포인터 변수를 선언하고 배열의 이름으로 초기화를 수행하여 사용하는 것도 가능
  - 배열의 이름을 나타내는 변수는 기억 장소에 저장되지 않음
  - 별도의 포인터 변수를 선언한여 사용하는 것은 포인터 변수 자체가 메모리에 저장
- 포인터 연산
- 포인터 변수 자체에 적용할 수 있는 연산
- 포인터 변수 자체에 대해서는 제한적 연산만 허용
  - 배정 연산: 포인터 변수에 주소값을 배정하는 연산
  - 간접 참조 연산: 포인터 변수에 간접 참조연산자 '*'를 적용하는 연산으로 포인터 변수가 가리키는 값을 반환
  - 주소 연산: 포인터 변수가 저장된 메모리의 주소를 반환
  - 증감 및 정수 산술 연산
    - 포인터 변수가 가지고 있는 주소에 증감 및 산술 연산을 수행
    - 산술 연산은 정수의 덧셈 및 뺄셈 연산만 가능
    - 배열을 가리키는 포인터 변수는 증감 연산을 수행할 때 배열의 범위를 벗어나지 않도록 주의해서 사용
    - 배열 포인터 변수 간의 뺄셈은 배열의 요소의 상대적인 위치를 구하는데 사용
- 포인터 배열
  - 포인터 자체를 배열로 만들어 사용
  - 문자열의 배열을 처리할 떄 사용
- 함수 포인터
  - 함수의 시작 주소를 변수에 저장하여 사용
  - 프로그램은 일반적으로 컴파일러에 의해 함수 단위의 실행 파일로 만들어진다.
  - 하나의 프로그램에 여러 개의 함수가 존재할 경우 여러 개의 실행 파일이 만들어짐
  - 이 경우, 여러 개의 실행 파일들은 시작 주소에 의해 구분
  - 함수 시작 주소를 포인터 변수에 저장하여 사용하는 방법을 함수 포인터
  - 같은 형태의 함수가 여러 개 있을 떄 하나의 함수 포인터로 여러 개의 함수를 호출하여 사용할 경우 유용
