# TIL: MVC Pattern, DI
## 학습 날짜: 2025년 2월 12일
## 오늘 내가 새롭게 배운 것들
**MVC Pattern**
- Layered Architecture(4-tier)
  - Presentation Layer: 입출력 처리
  - Business (Logic) Layer: 로직 처리
  - Persistence Layer: 데이터베이스 처리
  - Database Layer: 실제 데이터베이스
  - 각 층은 인접한 층이랑만 데이터 통신 가능
- MVC(Model-View-Controller) Pattern
  - Model
    - Data Model: VO
    - Service Model(Business Layer)
      - 로직 처리를 하는 객체
      - DB 처리는 하지 않음
      - Transaction 담당
  - View
    - Presentation Layer에 해당
    - 입출력을 담당
  - Controller
    - View와 Service Model 사이에 위치하여 둘을 연결
    - View에서 입력된 값을 적절한 Model에 전달해주고 출력값을 View로 보내주는 역할
  - DAO(Data Access Object)
    - Persistence Layer에 해당
    - 서비스 모델이 직접 데이터베이스를 처리할 수 없기 때문에 데이터베이스 처리를 위한 객체
    - 비즈니스 로직 처리를 하지 않음
    - DB Access 처리만 허용

**DI(Dependency Injection)**
- Step1. 일반적인 DAO
  ```
  public class UserDAO {
      // 새로운 사용자 등록
      public void insert(UserVO user) {
          // 데이터베이스 처리 6단계
          try {
              Class.forName("oracle.jdbc.driver.OracleDriver");
  
              String url = "jdbc:oracle:thin:@localhost:1521:xe";
              String id = "C##DEV";
              String pw = "4587";
  
              Connection conn = DriverManager.getConnection(url, id, pw);
  
              String sql = "INSERT INTO users VALUES (?,?,?)";
              PreparedStatement pstmt = conn.prepareStatement(sql);
              pstmt.setString(1, user.getId());
              pstmt.setString(2, user.getName());
              pstmt.setString(3, user.getPassword());
  
              pstmt.executeUpdate();
  
              pstmt.close();
              conn.close();
          } catch (Exception e) {
              System.out.println(e.getMessage());
          }
      }
  
      // 이하 생략...
  }

  ```
  - 데이터베이스를 연결하는 부분에서 중복된 코드가 발생 => 유지보수성이 떨어짐
- Step2. 중복된 코드 Method Extraction
  ```
  public class UserDAO {
  
      // 공통된 코드는 별도의 메서드로 분리(Method Extraction)
      private Connection getConnection() throws SQLException, ClassNotFoundException {
          Class.forName("oracle.jdbc.driver.OracleDriver");
  
          String url = "jdbc:oracle:thin:@localhost:1521:xe";
          String id = "C##DEV";
          String pw = "4587";
  
          return DriverManager.getConnection(url, id, pw);
      }
  
      // 새로운 사용자 등록
      public void insert(UserVO user) {
          // 데이터베이스 처리 6단계
          try {
              Connection conn = getConnection();
  
              String sql = "INSERT INTO users VALUES (?,?,?)";
              PreparedStatement pstmt = conn.prepareStatement(sql);
              pstmt.setString(1, user.getId());
              pstmt.setString(2, user.getName());
              pstmt.setString(3, user.getPassword());
  
              pstmt.executeUpdate();
  
              pstmt.close();
              conn.close();
          } catch (Exception e) {
              System.out.println(e.getMessage());
          }
      }
  
      // 이하 생략...
  }
  ```
  - 유지보수성 측면에서 조금 더 나아짐
- Step3. 추상 메서드와 추상 클래스로 만들어서 제공
  UserDAO.java
  ```
  // 재활용성을 위해 추상 클래스로 변경
  public abstract class UserDAO {
  
      // 공통된 코드는 별도의 메서드로 분리(Method Extraction)
      // 재활용성을 위해 추상 메서드로 변경
      public abstract Connection getConnection();
  
      // 새로운 사용자 등록
      public void insert(UserVO user) {
          // 데이터베이스 처리 6단계
          try {
              Connection conn = getConnection();
  
              String sql = "INSERT INTO users VALUES (?,?,?)";
              PreparedStatement pstmt = conn.prepareStatement(sql);
              pstmt.setString(1, user.getId());
              pstmt.setString(2, user.getName());
              pstmt.setString(3, user.getPassword());
  
              pstmt.executeUpdate();
  
              pstmt.close();
              conn.close();
          } catch (Exception e) {
              System.out.println(e.getMessage());
          }
      }
  
      // 이하 생략...
  }
  ```
  KUserDAO.java
  ```
  public class KUserDAO extends UserDAO {
      // 추상 클래스를 상속을 해서 사용하는 측에서 이 메서드를 자신에게 맞게
      // Overriding 해서 사용
      @Override
      public Connection getConnection() {
          Connection conn = null;
          try {
              Class.forName("oracle.jdbc.driver.OracleDriver");
  
              String url = "jdbc:oracle:thin:@localhost:1521:xe";
              String id = "C##DEV";
              String pw = "4587";
  
              conn = DriverManager.getConnection(url, id, pw);
          } catch (Exception e) {
              System.out.println(e.getMessage());
          }
  
          return conn;
      }
  }
  ```
  - 사용자는 상속해서 사용 가능 (KUserDAO가 UserDAO를 상속)
  - 재활용성의 측면에서 나아짐
  - Template Method Pattern
    - 상위 클래스에서 기본적인 로직의 흐름을 구현
    - 하위 클래스에서 구체적인 구현 내용을 변경해서 구현
  - 자바는 다중 상속이 불가능하기 때문에 이미 상속하고 있는 클래스에 붙여쓸 수 없는 문제가 있음
- Step4. UserDAO의 변경해야하는 부분을 별도의 코드로 분리
  SimpleConnectionMaker.java
  ```
  public class SimpleConnectionMaker {
  
      public Connection makeNewConnection() throws Exception{
          Class.forName("oracle.jdbc.driver.OracleDriver");
  
          String url = "jdbc:oracle:thin:@localhost:1521:xe";
          String id = "C##DEV";
          String pw = "4587";
  
          Connection conn = DriverManager.getConnection(url, id, pw);
  
          return conn;
      }
  }
  ```
  UserDAO.java
  ```
  public class UserDAO {
  
      private SimpleConnectionMaker connectionMaker;
  
      public UserDAO() {
          connectionMaker = new SimpleConnectionMaker();
      }
  
      // 새로운 사용자 등록
      public void insert(UserVO user) {
          // 데이터베이스 처리 6단계
          try {
              Connection conn = connectionMaker.makeNewConnection();
  
              String sql = "INSERT INTO users VALUES (?,?,?)";
              PreparedStatement pstmt = conn.prepareStatement(sql);
              pstmt.setString(1, user.getId());
              pstmt.setString(2, user.getName());
              pstmt.setString(3, user.getPassword());
  
              pstmt.executeUpdate();
  
              pstmt.close();
              conn.close();
          } catch (Exception e) {
              System.out.println(e.getMessage());
          }
      }
  
      // 이하 생략...
  }
  ```
  - 클래스 내에 다른 클래스의 이름이 명시적으로 박혀있음
  - UserDAO를 재사용하려면 반드시 UserDAO에 명시적으로 적힌 class를 만들어서 사용해야 함
- Step5. 
