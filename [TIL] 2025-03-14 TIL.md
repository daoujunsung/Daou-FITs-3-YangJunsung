# TIL: 구조체
## 학습 날짜: 2025년 3월 14일
## 오늘 내가 새롭게 배운 것들
**구조체**
- 서로 다른 형태의 데이터를 여러 개 모아서 하나의 자료구조로 표현
- 구조체로 만들어진 자료는 연속된 메모리에 할당
- 선언 형식
  ```
  struct 구조체이름 {
    자료형 식별자1;
    자료형 식별자2;
    ...
  };
  ```
- 구조체 사용
  ```
  struct 구조체이름 구조체변수;

  // 구조체 선언과 동시에 구조 변수 지정
   struct 구조체이름 {
    자료형 식별자1;
    자료형 식별자2;
    ...
  } 구조체변수1, 구조체변수2;
  ```
  - typedef는 모든 자료형을 사용자가 지정하는 자료형으로 가독성과 이식성을 향상
    ```
    typedef unsigned char BYTE; // unsigned char를 새로운 BYTE라는 형식으로 정의
    BYTE b1 = 200;
  
    typedef struct student STU; // 구조체 student를 STU로 선언
    STU student1; // STU형의 구조체 변수 student1 선언
  
    // typedef를 사용하여 구조체 st_score 전체를 SCO형으로 정의
    typedef struct st_score {
    int id; int kor; double avg; } SCO;
    SCO student1, student2; // SCO형의 구조체 변수 student1, student2 선언
    ```
- 구조체 초기화
  - 구조체 변수를 선언하면서 멤버의 값을 일괄 초기화하기
  ```
  struct student {
    char name[20];
    int kor;
  };
  struct student st1 = {"hee", 100};

  struct student {
    char name[20];
    int kor;
  } st1 = {"hee", 100};

  typedef struct student {
    char name[20];
    int kor;
  } STU;
  STU st1 = {"hee", 100};
  ```
  - 구조체에 대해 멤버 단위로 초기화 수행하기
  ```
  struct student {
    char name[20];
    int kor;
  };
  struct student st1;
  strcpy(st1.name, "hee");
  st1.kor = 100;
  ```
