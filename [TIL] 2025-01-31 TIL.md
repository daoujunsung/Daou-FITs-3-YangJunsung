# TIL: SQL 프로젝트
## 학습 날짜: 2025년 1월 31일
## 어떤 문제가 있었는지
- 여러 컬럼을 PK로 동시에 지정해야하는 문제가 생겼다
- 다대다 관계가 필요한 테이블이 있었다.
## 그 문제를 해결해보기 위해 내가 시도해본 것들
- PRIMARY KEY 옵션을 두개 다 넣어봤지만, 오류가 발생했다.
- ADD CONSTRAINT를 이용해 제약사항을 추가해주었다.
- 다대다의 경우 양쪽에 모두 FK를 지정해보았지만 되지 않았다.
- 중계 테이블을 활용하였다.
## 어떻게 그 문제를 해결했는지
- 제약사항을 명시적으로 추가함으로써 해결하게 되었다.
- 중계 테이블을 통해 해결할 수 있었다.
## 오늘 내가 새롭게 배운 것들
- 복합키를 PK로 사용할 때는 CREATE TABLE에 PRIMARY KEY라는 옵션을 이용하는 것이 아니라 ALTER TABLE을 통해 제약 조건을 추가해주는 방식으로 지정 가능하다.
- 다대다 관계를 표현하기 위해선 중계테이블을 이용하여 각각의 테이블을 중계테이블과 일대다 관계로 연결하여 표현 가능하다.
