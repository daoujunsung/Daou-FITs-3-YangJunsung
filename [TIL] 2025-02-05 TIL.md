# TIL: 객체 지향
## 학습 날짜: 2025년 2월 5일
## 오늘 내가 새롭게 배운 것들
**클래스**
- 객체 모델링의 수단
- 사용자 정의 데이터 타입

**인스턴스**
- 클래스에 정의된 필드와 메서드를 사용하기 위해 메모리 상에 생성된 일정 공간

**생성자**
- 모든 클래스는 적어도 하나 이상의 생성자를 가짐
- 생성자의 이름은 클래스의 이름과 동일

**JVM의 메모리 구조**
- Register: 실행할 코드를 지시해주는 포인터를 관리
- Runtime Constant Pool: 실행 시 상수로 정의된 것을 저장해두는 공간, Heap의 일부분
- Method Area: 클래스를 사용할 때 해당 클래스에 대한 정보와 static으로 정의된 필드, 메서드의 바이트코드를 저장하는 공간
- Heap: 인스턴스를 저장하는 공간
- Call Stack: 메서드를 실행하기 위해 필요한 변수 등을 저장하는 공간, 메서드 실행이 끝나면 제거

**Class Member & Instance Member**
- Class Member: static keyword로 선언된 모든 인스턴스가 공유하는 필드와 메서드
- Instance Member: 각 인스턴스가 독립적으로 메모리 공간을 확보하는 필드와 메서드, 인스턴스를 이용해야만 사용 가능

**Method Overloading**
- 같은 이름의 메서드가 여러 개 존재하는 것
- 매개변수의 개수와 데이터 타입을 이용하여 구분
- 생성자도 적용

**Package & Import**
- 소스 코드를 효율적으로 관리하고 재사용하는 방법
- 패키지라는 논리적인 개념을 폴더라는 물리적 형태로 표현
- 다른 class를 사용할 때는 반드시 class의 package를 명시해야하는데 이를 편하게 사용하기 위한 구문이 import

**Access Modifier(접근제어자)**
- public: 어디에서나 접근 가능한 field, method, constructor, class 선언에 이용
  - 하나의 자바 파일 안에 두개 이상의 public class가 존재할 수 없음
- protected: 기본적으로 default와 같지만 상속 관계에 있는 class에서 field, method, constructor를 사용할 수 있도록 선언
- default: 같은 package 내의 다른 class에서 접근 가능한 field, method, constructor, class 선언
- private: 다른 class에서는 접근할 수 없는 field, method, constructor 선언

**Static Block**
- class가 로드된 후 main thread에 의해 main method가 호출되기 이전에 실행

**Inheritance(상속)**
- 클래스가 2개가 등장
- 부모 클래스(Parent class, Super class) = 상위 클래스(Upper class)
- 자식 클래스(Child class, Sub class) = 하위 클래스(Lower class)
- 부모 클래스가 가진 내용을 자식 클래스가 물려받는 것
- 자바는 단일 상속만 가능
- 클래스들은 상속에 상속을 이어가며 확장되어 만들고 사용됨
- 자바의 모든 클래스는 상속 관계에 있음 (java.lang.Object 클래스를 상속)
- 부모와 자식이 tightly coupled 되기 때문에 재사용성은 좋아지지만 재활용성은 떨어지게 됨
