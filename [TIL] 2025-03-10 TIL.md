# TIL
## 학습 날짜: 2025년 3월 10일
## 오늘 내가 새롭게 배운 것들
**전역 변수**
- 함수 밖에서 선언한 변수
- 모든 영역(프로그램 전체)에서 참조할 수 있는 변수
- 프로그램 상단에 선언하는 것이 편리
- 프로그램 실행 시작부터 종료까지 기억장소 차지
- 장점
  - 함수 간 인수 전달이 필요 없다
  - 기억 공간을 효율적으로 사용
- 단점
  - 다양한 인수에 대해 같은 기존의 함수를 활용할 수 없다
  - 함수의 재사용성이 낮아짐
  - 부작용 발생 가능: 전역변수의 값을 변경하면 그 결과가 모든 함수에 영향을 미침
  - 극복하기 위한 방법: 지역 변수를 인수로 전달하는 것이 바람직
  
**배열**
- 여러 개의 변수를 나란히 연결한 것
- 형식: 데이터 형식 배열_이름[개수] ex. int arr[4]
- 배열의 첨자가 순서대로 변할 수 있도록 반복문과 함께 활용
- 배열의 초기화
  - 배열을 정의하는 동시에 값을 대입하는 것 int arr[4] = {1,2,3,4};
  - 배열의 개수를 지정하지 않으면 블록 {} 안의 초기값의 개수에 따라 자동으로 배열 개수가 정해짐
  - 배열을 선언하기만 하고 초기화하지 않으면 각 배열에 아무것도 넣지 않았기 때문에 쓰레기가 들어감
  - 배열의 개수보다 초기화 값이 적으면 0이 채워짐
  - 배열의 개수보다 초기화 값이 많으면 오류 발생
- 배열의 크기 = sizeof(배열) / sizeof(데이터 형식)
- 2차원 배열: 1차원 배열을 여러 개 연결한 것
