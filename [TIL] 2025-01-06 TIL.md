# TIL
## 학습 날짜: 2025년 1월 6일
## 오늘 내가 새롭게 배운 것들
**이산수학**
: 불연속적인 객체를 연구하는 수학
- 집합(Set) 
- 관계(Relation)    
- 함수(Function)    
- 명제(Proposition)    
- 그래프(Graph)    
- 트리(Tree)    

**집합**
: 이산 수학에서 고려되는 객체 유형에 대한 기본 구성 요소, 순서가 지정되지 않은 원소 모임
- 전체집합 : 모든 원소를 포함한 집합
- 공집합 : 아무 원소도 없는 집합
- 부분집합 : 집합 내의 모든 원소가 어떤 집합에 모두 속한 집합
- 멱집합 : 어떤 집합의 가능한 모든 부분집합의 집합
- 합집합 : 두 집합 중 하나라도 속한 원소들의 집합
- 교집합 : 두 집합 모두 속한 원소들의 집합
- 여집합 : 전체집합의 원소들 중 어떤 집합에 속하지 않은 원소들의 집합
- 차집합 : 두 집합 중 하나엔 속해있고 하나엔 속하지 않은 원소들의 집합
- 순열(Permutation) : 서로 다른 원소들을 순서를 고려하여 일렬로 배열하는 것, P(n,r) = n!/{(n-r)!}
- 조합(Combination) : 서로 다른 원소들을 순서를 고려하지 않고 일렬로 배열하는 것, C(n,r) = n!/{(n-r)!*r!}, C(n,r) = C(n, n-r)

**함수**
: 정의역(Domain) X의 모든 원소에 대해 공역(Codomain) Y의 하나의 원소만 대응될 때, f:X->Y
- 상(Image)과 원상(Preimage) : f(a) = b이면 b는 a의 상, a는 b의 원상
- 치역(Range) : 상들의 집합
- 일대일 함수(Injective Function) : 함수의 각 입력 값에 대해 유일한 출력값이 대응되는 함수, 서로 다른 입력 값들은 서로 다른 출력을 가짐
- 단사(Injection) : 일대일 함수와 같은 의미, 정의역의 서로 다른 원소들이 공역의 서로 다른 원소에 대응되는 함수
- 전사(Surjection) : 공역의 모든 원소가 적어도 하나의 정의역 원소에 대응, 공역 = 치역
- 전단사(Bijections) : 단사이면서 전사인 경우

**관계**
: 함수보다 더 일반적인 성격
- 이진 관계(Binary Relation) : 두 집합의 카타시안 곱 형태, Pair를 이루게 됨
- 반사적인 관계(Reflexive Relation) : 관계가 정의된 집합의 모든 원소는 자기 자신과 관계를 가짐
- 대칭적인 관계(Symmetric Relation) : 관계가 정의된 집합에서 원소 a와 b가 관계를 가지면 b와 a도 관계를 가짐
- 추이적 관계(Transitive Relation) : a와 b, 그리고 b와 c가 관계를 가지면 a와 c도 관계를 가짐
 
**명제**
: 참이나 거짓으로 명확히 판별될 수 있는 문장, 논리연산자와 다른 명제로 구성
- 부정(Negation) : not
- 논리곱(Conjunction) : 그리고(and)
- 논리합(Disjunction) : 또는(or)
- 배타적 논리합(Exclusive Or, Xor) : 둘 중에 하나만 참이고 하나는 거짓
- 조건(Implication) : p -> q 일 때, p가 참이면 q가 참이어야 함. p가 참이지만 q가 거짓인 경우만 거짓을 출력
- 상호조건(Biconditional) : p <-> q 일 때, p가 참이면 q가 참이어야하고, q가 참이면 p가 참이어야 함. 둘의 결과가 같을 때만 참을 출력
- 논리연산자 우선순위 : 부정 > 논리곱 > 논리합 > 조건 > 상호조건
- 진리표(Truth Table) : 나올 수 있는 모든 조합을 표시한 표
- 항진명제(Tautology) : 항상 참인 명제
- 모순명제(Contradiction) : 항상 거짓인 명제
- 역(Converse) : p -> q의 역은 q -> p
- 이(Inverse) : p -> q의 이는 ￢p -> ￢q
- 대우(Contrapositive) : p -> q의 대우는 ￢q -> ￢p
- 논리적 동치(Logically Equivalent) : 두 명제가 항상 동일한 진리 값을 가지는 경우 . ex) 대우, ￢p∨q
- 논리적 비동치(Logically Non-Equivalent) : 두 명제가 항상 동일한 진리 값을 가지지는 않는 경우. ex) 역, 이
- 드 모르간의 법칙(De Morgan's Law) : ￢(p∧q) ≡ ￢p∨￢q,  ￢(p∨q) ≡ ￢p∧￢q
- 항등 법칙(Identity Law) : p∧T ≡ p, p∨F ≡ p
- 지배 법칙(Domination Law) : p∨T ≡ T, p∧F ≡ F
- 멱등 법칙(Idempotent Law) : p∧p ≡ p, p∨p ≡ p
- 이중부정(Double Negation) : ￢(￢p) ≡ p
- 부정(Negation Law) : p∨￢p ≡ T, p∧￢p ≡ F
- 교환 법칙(Commutative Law) : p∧q ≡ q∧p, p∨q ≡ q∨p
- 결합 법칙(Associative Law) : (p∧q)∧r ≡ p∧(q∧r), (p∨q)∨r ≡ p∨(q∨r)
- 분배 법칙(Distributive Law) : p∨(q∧r) ≡ (p∨q)∧(p∨r), p∧(q∨r) ≡ (p∧q)∨(p∧r)
- 흡수 법칙(Absorption Law) : p∨(p∧q) ≡ p, p∧(p∨q) ≡ p

**부울 대수(Boolean Algebra)**
- 부울식(Boolean Expression) : 부울대수에서 사용되는 수학 표현식, 참과 거짓 두 값만 사용할 수 있는 변수를 포함
- 연산 순서 : () > NOT > AND > OR
- 공리(Axioms of Boolean Algebra) : AND(·, 둘다 1일 때만 1), OR(+, 둘 중 하나만 1이면 1), NOT(', 1이면 0, 0이면 1)
- 이중보수법칙 : (x')' = x
- 멱등 법칙 : x + x = x, x · x = x
- 항등 법칙 : x + 0 = x, x · 1 = x
- 지배 법칙 : x + 1 = 1, x · 0 = 0
- Unit Property : x + x' = 1
- Zero Property : x · x' = 0
- 교환 법칙 : x + y = y + x, x · y = y · x
- 결합 법칙 : x + (y + z) = (x + y) + z, x · (y · z) = (x · y) · z
- 분배 법칙 : x + y · z = (x + y) · (x + z), x · (y + z) = x · y + x · z
- 드 모르간의 법칙 : (x · y)' = x' + y', (x + y)' = x' · y'
- 흡수 법칙 : x + x · y = x, x · (x + y) = x
- 쌍대법칙(Principle of Duality) : Dual(+ -> ·, · -> +, 1 -> 0, 0 -> 1로 바꾼 것), 참의 dual도 참
- 부울 함수(Boolean Function) : 논리값인 0과 1만을 사용하는 함수
- 표준형(Canonical Form) : 부울 함수를 표준화된 형태로 표현하는 방법, Sum of Minterms(SOM), Product of Maxterms(POM)
- Minterm : 입력 변수들의 모든 가능한 조합(곱)
- SOM(Sum of Minterms) : f = ∑ m_i (m_i는 f=1이 되는 경우의 minterm)
- SOP(Sum of Products) : SOM = simplified SOP
- Maxterm : minterm의 complement
- POM(Product of Maxterms) : f = ∏ M_i (M_i는 f=0이 되는 경우의 maxterm)
