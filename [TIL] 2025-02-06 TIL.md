# TIL: 객체 지향, 주요 class
## 학습 날짜: 2025년 2월 6일
## 오늘 내가 새롭게 배운 것들
**상속과 관련된 constructor의 특징**
- constructor는 상속되지 않음
- 인스턴스를 생성할 때, 상위 클래스의 인스턴스를 먼저 생성 -> super()
- super()가 호출되기 전에 생성자에서 다른 작업을 할 수 없음

**Method Overriding**
- 부모로부터 물려받은 method의 내용을 하위 class에서 다시 정의하는 것
- method의 이름이 같아야 함
- return type이 같아야 함
- parameter가 같아야 함
- access modifier는 부모의 것과 같거나 더 넓어야 함

**Is-A Relationship**
- Sub class is a Super class
- 역은 성립하지 않음
- sub class type을 써야 하는 자리에 super class의 type을 사용 가능

**this, super, this(), super()**
- this: this keyword가 사용된 instance에 대한 reference
- super: this와 같은 의미이지만 실제로 가리키는 객체는 상위 클래스의 객체
- this(): constructor 내에서 overloading된 다른 constructor를 호출하기 위해 사용
- super(): constructor 내에서 상위 class의 constructor를 호출하기 위해 사용

**Inheritance Order Test**
- SuperClass.java
  ```
    public class SuperClass {
  
      static int staticCall(String msg) {
          System.out.println(msg);
          return 0;
      }
  
      int a = staticCall("1번 문장입니다.");
      static int b = staticCall("2번 문장입니다.");
  
      public SuperClass() {
          staticCall("3번 문장입니다.");
      }
  
      public SuperClass(int i) {
          this();
          staticCall("4번 문장입니다.");
      }
  
      public void myFunc() {
          System.out.println("5번 문장입니다.");
      }
  }
  ```
- Exam01_InheritanceOrder
  ```
    public class Exam1_InheritanceOrder extends SuperClass{
  
      int c = staticCall("6번 문장입니다.");
      static int d = staticCall("7번 문장입니다.");
  
      public Exam1_InheritanceOrder() {
          super(100);
          staticCall("8번 문장입니다.");
          super.myFunc();
      }
  
      @Override
      public void myFunc() {
          System.out.println("9번 문장입니다.");
      }
  
      public static void main(String[] args) {
          System.out.println("10번 문장입니다.");
          SuperClass obj = new Exam1_InheritanceOrder();
          obj.myFunc();
      }
  }
  ```
- 답: 2 -> 7 -> 10 -> 1 -> 3 -> 4 -> 6 -> 8 -> 5 -> 9

**Abstraction (추상화)**
- 객체들의 공통적인 특징(속성과 기능)을 뽑아내서 이름을 붙이는 것
- class를 정의하는 과정 그 자체

**Encapsulation(캡슐화)**
- 데이터 구조와 데이터를 다루는 방법을 결합시켜 묶은 것
- class 그 자체
- class의 재활용성을 높이고 에러 발생을 최소화

**Information Hiding(정보 은닉)**
- class는 외부에서 알 필요가 없는 field나 method를 외부에 노출 시킬 필요가 없음
- access modifier를 이용하여 선언된 class의 외부에서의 접근을 제어하면서 정보를 은폐하는 방식

**final**
- class: 해당 class는 상속할 수 없음
- field: 변경할 수 없는 field(상수)
- method: 하위 class에서 overriding 할 수 없음

**Abstract class(추상 클래스)**
- instance를 생성할 수 없음
- abstract method를 하위 클래스에서 overriding 해서 instance를 생성

**Polymorphism(다형성) & Dynamic Binding(동적 바인딩)**
- 다형성은 부모의 객체 타입으로 다양한 형태의 자식 객체를 생성, 사용할 수 있는 것을 의미
- 동적 바인딩이란 상위 클래스로 타입을 지정해도 마지막에 overriding된 메서드로 실행되는 것을 의미

**Interface(인터페이스)**
- abstract method와 public static final field로 구성
- implements 키워드를 이용하여 구현
- interface 간의 상속 가능
- interface와 구현한 class 사이에도 is-a relationship 성립
- 상속과 다르게 다중 구현이 가능

**Object**
- 모든 class의 최상위 class
- 주요 메서드
  - equals(): 같은 값인지 비교하는 메서드
