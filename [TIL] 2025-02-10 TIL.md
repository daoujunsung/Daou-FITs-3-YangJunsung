# TIL: JDBC
## 학습 날짜: 2025년 2월 10일
## 오늘 내가 새롭게 배운 것들
**JDBC(Java Database Connectivity)**
- Java를 이용하여 Database에 있는 값을 활용하기 위한 API
- JDBC Driver: Java 응용 프로그램이 데이터베이스와 상호작용할 수 있도록 하는 소프트웨어 구성 요소(Class & Interface)
- JDBC는 크게 6가지 단계로 사용
  - (JDBC) Driver Loading: 프로그램에서 딱 1번만 수행 
  - 데이터베이스 연결
    - 만약 연결이 성공하면 연결 성공의 증표로 객체가 생성
    - 객체의 class는 java.sql.Connection
    - 연결을 하려면 3가지 정보가 필요
      - 어떤 데이터베이스에 접속하는 지에 대한 정보(문자열) => JDBC URL
      - ID / PW 필요
    - SQL 작성 => Statement 생성
      - Statement 종류는 3개
        - 일반 Statement: 단순, 기능상으로는 약간 부족, 잘 안쓰임
        - PreparedStatement: 코드 작성이 편하고 효율이 좋으면서 SQL Injection을 방지할 수 있어 가장 많이 사용됨
        - CallableStatement: Stored Procedure 호출을 위해 사용
    - Query 실행
      - executeUpdate(): SQL문이 insert, update, delete 계열일 때 사용
      - executeQuery(): SQL문이 select 문일 때 사용
    - 결과 처리
      - executeUpdate(): 리턴값은 영향을 받은 행의 수(int)
      - executeQuery(): Resultset이라는 형태로 결과 반환, rs.next()와 rs.getString() 등의 함수를 이용하여 java가 처리할 수 있도록 가공
    - 리소스 해제
      - Connection, PreparedStatement, Resultset 등 사용된 자원을 명시적으로 해제(close()로 호출)해야함
- 트랜잭션을 JDBC를 이용해서 설정하려면 Connection에 설정
  - 기본적으로 autoCommit mode로 Connection이 생성
  - 만약 트랜잭션을 설정하려면 ```conn.setAutoCommit(false);```
  - 명시적으로 conn.commit() 혹은 conn.rollback()이 호출되면 트랜잭션이 종료
  - 만약 해당 구문이 나오지 않고 connection이 정상 종료되면 conn.commit()으로 간주
