# TIL: Java의 특징과 데이터 타입
## 학습 날짜: 2025년 2월 4일
## 오늘 내가 새롭게 배운 것들
**Java 언어의 특징**
- 플랫폼에 독립적
  - 하이 레벨 랭귀지로 작성한 코드를 컴파일러를 통해서 머신코드로 변경하는 것이 일반적
  - 자바는 약간 다르게 컴파일러를 통해서 바이트 코드로 변경 
  - 이렇게 변경된 바이트 코드를 JVM이 읽어들여서 머신코드로 변경해서 실행
  - .java -> (javac compiler) -> .class -> (JVM)
  - JVM은 플랫폼에 종속적
- 객체지향 언어
  - 절차적 언어
    - 기능 단위로 프로그램을 세분화
    - 더이상 분할할 수 없는 단위 기능을 함수로 구현
    - 설계와 구현이 쉽고 결과적으로 개발 비용 감소
    - 유지보수가 어려움
    - 대표적인 언어는 C
  - 객체지향 프로그래밍: 절차적 언어의 유지보수의 어려움을 해결하기 위한 패러다임
  - 해결해야 하는 현실 세계를 프로그램으로 모델링하는 작업을 한 후에 프로그래밍 진행
  - 설계가 어려움
- JVM(Java Virtual Machine): Bytecode를 읽어들여서 실행하는 실행 주체
- JRE(Java Runtime Environment): 자바 실행 환경, JVM + Java Class Library
- JDK(Java Development Kit): 자바 개발 도구, JRE + 자바 개발 도구(javac.exe, java.exe, javadoc.exe, etc.)

**Java의 객체 지향**
- 현실 세계의 구현해야 하는 문제를 프로그래밍적으로 표현(모델링)을 하는 구현 방법론
- 해결해야 하는 문제를 구성하고 있는 구성 요소를 파악
- 추상화(Abstraction)을 거쳐서 나오는 여러가지 정보를 상태(ex. 잔액, 주소, 주민번호,...)와 행위(ex. 입금, 출금, ...)로 구분
- 추상화: 프로그램에 필요한 정보만을 추려내는 행위
- 상태는 변수(Field)로 행위는 함수(Method)로 표현
- 클래스(Class): 객체 모델링의 수단, 필드와 메서드를 묶어둔 것
- 자바 프로그램은 클래스의 집합으로 표현 가능
- 코드로 표현할 때 클래스 1개당 파일 1개
- 클래스의 이름을 파일 이름으로 명명
```
// 클래스 이름은 첫글자 반드시 대문자
class Customer {
    // fields
    String name; // 고객 이름
    int age;     // 고객 나이

    // methods
    
}
```
- 클래스는 결국 데이터 타입의 역할을 수행
- 고정된 데이터 타입이 아니기 때문에 추상 데이터 타입(ADT, Abstract Data Type)이라고 함
- 이 클래스로부터 저장 공간을 생성해서 그 저장공간을 이용해서 프로그래밍을 진행
- 이 저장 공간을 Instance라고 부름
- 클래스는 Instance를 파생시키는 수단
- 패키지(Package)
  - 클래스들을 효율적으로 관리하기 위해 사용하는 논리적인 개념
  - 특정 기준으로 묶어둔 클래스들의 집합
  - 물리적으로 폴더로 표현

**메모리 구조**
- Method Area: 실행되면 클래스에 대한 정보가 저장
- Stack: 실행이 되면 모든 메서드가 이 영역에 저장
- Heap: 그 외의 모든 게 저장
  
**데이터 타입**
- 변수
  - 자바는 대표적인 강타입언어(Strong Type Language)이기 때문에 변수를 선언할 때 변수의 데이터 타입을 지정
  - 식별자 명명 규칙
    - 길이의 제한이 없음, 대소문자 구분
    - 예약어로는 변수를 만들 수 없음
    - 숫자로 시작할 수 없음
    - 특수문자는 '_'와 '$'만 허용
    - 클래스 이름은 첫글자 항상 대문자 (Pascal case) ex) MyFriend
    - 일반 변수 이름은 Camel case ex) myFriend
- 데이터 타입은 크게 2가지로 나뉨(Primitive data type, Reference data type)
  - 기본형(Primitive data type): 해당 타입으로 선언된 변수 안에 직접 데이터가 저장
    - 논리형: boolean
    - 문자형: char
    - 정수형: byte(1 byte), short(2 byte), int(4 byte), long(8 byte)
    - 실수형: float, double
  - 참조형(Reference data type): 클래스로 선언된 변수, 클래스는 heap 영역에 저장이 되고 그 메모리 주소의 해시값이 stack 영역에 저장
- 상수: 항상 같은 값을 가지는 공간, final이라는 키워드로 상수 표현
- 형변환: 하나의 데이터 타입이 다른 데이터 타입으로 변환하는 걸 의미
  - 묵시적 형변환(Implicit)
  - 명시적 형변환(Casting)
 
**Overloading**
- 같은 이름이나 같은 형태로 여러 개의 기능을 수행하는 경우
- 예시 (연산자 오버로딩)
  - +
    - 앞과 뒤에 숫자가 나오면 두 숫자를 합
    - 앞과 뒤 둘 중 하나 혹은 모두 문자열이면 concat
- 이 개념을 메서드에도 적용한 개념이 Method Overloading
- 이 개념을 생성자에도 적용하면 Constructor Overloading
