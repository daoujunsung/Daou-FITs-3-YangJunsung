# TIL
## 학습 날짜: 2025년 2월 4일
## 오늘 내가 새롭게 배운 것들
**Java 언어의 특징**
- 플랫폼에 독립적
  - 하이 레벨 랭귀지로 작성한 코드를 컴파일러를 통해서 머신코드로 변경하는 것이 일반적
  - 자바는 약간 다르게 컴파일러를 통해서 바이트 코드로 변경 
  - 이렇게 변경된 바이트 코드를 JVM이 읽어들여서 머신코드로 변경해서 실행
  - .java -> (javac compiler) -> .class -> (JVM)
  - JVM은 플랫폼에 종속적
- 객체지향 언어
  - 절차적 언어
    - 기능 단위로 프로그램을 세분화
    - 더이상 분할할 수 없는 단위 기능을 함수로 구현
    - 설계와 구현이 쉽고 결과적으로 개발 비용 감소
    - 유지보수가 어려움
    - 대표적인 언어는 C
  - 객체지향 프로그래밍: 절차적 언어의 유지보수의 어려움을 해결하기 위한 패러다임
  - 해결해야 하는 현실 세계를 프로그램으로 모델링하는 작업을 한 후에 프로그래밍 진행
  - 설계가 어려움
- JVM(Java Virtual Machine): Bytecode를 읽어들여서 실행하는 실행 주체
- JRE(Java Runtime Environment): 자바 실행 환경, JVM + Java Class Library
- JDK(Java Development Kit): 자바 개발 도구, JRE + 자바 개발 도구(javac.exe, java.exe, javadoc.exe, etc.)

**Java의 객체 지향**
- 현실 세계의 구현해야 하는 문제를 프로그래밍적으로 표현(모델링)을 하는 구현 방법론
- 해결해야 하는 문제를 구성하고 있는 구성 요소를 파악
- 추상화(Abstraction)을 거쳐서 나오는 여러가지 정보를 상태(ex. 잔액, 주소, 주민번호,...)와 행위(ex. 입금, 출금, ...)로 구분
- 추상화: 프로그램에 필요한 정보만을 추려내는 행위
- 상태는 변수(Field)로 행위는 함수(Method)로 표현
- 클래스(Class): 객체 모델링의 수단, 필드와 메서드를 묶어둔 것
- 자바 프로그램은 클래스의 집합으로 표현 가능
- 코드로 표현할 때 클래스 1개당 파일 1개
- 클래스의 이름을 파일 이름으로 명명
```
// 클래스 이름은 첫글자 반드시 대문자
class Customer {
    // fields
    String name; // 고객 이름
    int age;     // 고객 나이

    // methods
    
}
```
- 클래스는 결국 데이터 타입의 역할을 수행
- 고정된 데이터 타입이 아니기 때문에 추상 데이터 타입(ADT, Abstract Data Type)이라고 함
- 이 클래스로부터 저장 공간을 생성해서 그 저장공간을 이용해서 프로그래밍을 진행
- 이 저장 공간을 Instance라고 부름
- 클래스는 Instance를 파생시키는 수단
