# TIL
## 학습 날짜: 2025년 2월 21일
## 어떤 문제가 있었는지
1. ajax를 통해 댓글을 등록하면 boardId에 대한 정보를 잃고 boardDetail 페이지가 500 에러가 발생, window.location.href로 해결했었지만 ajax에 맞지 않음
2. 버튼을 통해 댓글 삭제를 구현하는데 댓글 id를 받아올 속성이 마땅치 않음
## 그 문제를 해결해보기 위해 내가 시도해본 것들
1-1. response를 만들어서 새로 보내주는 방식을 이용해봄
1-2. 로그를 단계 단계 찍어보며 어디서 문제가 발생하는지 찾음
2-1. hidden input을 이용해 댓글 id 값을 넣어두려고 했지만 조금 어색함
## 어떻게 그 문제를 해결했는지
1. form 안의 button 태그가 submit 타입이라 리디렉션이 일어나던 것. button 타입으로 변경을 하니 해결
2. reply-id라는 새로운 속성을 만들어서 버튼 태그에 추가하고 그 값을 replyId를 지정해줌
