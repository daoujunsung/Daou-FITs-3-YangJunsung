# TIL: Servlet
## 학습 날짜: 2025년 2월 17일
## 오늘 내가 새롭게 배운 것들
**Servlet**
- Interface이기 때문에 직접적으로 구현하지 않음
- Servlet -> GenericServlet class -> HttpServlet Class를 상속해서 Servlet 구현
- 상속되는 메서드
  - init(): 서블릿 객체가 생성될 때 호출되는 초기화를 담당하는 메서드, 생성자 대신 init()을 이용해서 초기화 진행
  - service(): 클라이언트가 서블릿을 실행할 때 호출되는 메서드, 이미 구현되어 있어서 일반적으로 overriding 하지 않음     
    클라이언트의 요청이 GET 방식이면 doGet()을 호출하고 POST 방식이면 doPost()를 호출
  - doGet(), doPost(): 이 두개의 메서드를 오버라이딩해서 클라이언트가 원하는 작업을 수행
  - destroy(): Servlet Instance가 WAS에서 삭제될 때 수행되는 메서드, 사용하고 있는 여러 리소스들을 해제
- URL Mapping: 서블릿에 논리적인 이름을 부여해서 클라이언트가 서블릿을 호출할 수 있도록 하는 것
  - web.xml 파일 이용: XML 파일을 이용하기 때문에 번거롭지만, 전체적인 이름 관리를 하나의 파일에서 할 수 있어 관리 면에서 우수
  - @WebServlet 어노테이션 이용: 직관적이고 간편하기 때문에 일반적으로 사용
- PrintWriter를 이용해서 클라이언트에게 데이터 연결 통로를 열고 그 통로를 통해서 데이터를 전달
- 클라이언트가 서버 쪽 프로그램을 호출하는 2가지 방식
  - GET
    - Query String을 이용해서 데이터를 전달하는 방식
    - Query String: 특수한 형식을 가진 문자열(?변수1=값1&변수2=값2&변수3=값3)
    - 예시) http://localhost:8080/HelloServletProject2_war_exploded/myServlet?name=john&age=20&address=seoul
    - getParameter("변수명") 메서드를 통해 서블릿에서 데이터를 전달 받음
    - URL에 데이터가 노출되기 때문에 보안성이 전혀 없음 => 128bit 암호화를 이용해서 처리
    - URL 길이 제한 때문에 크기가 큰 데이터를 전송하기 힘듦
  - POST
    - URL에 데이터가 노출되지 않아 GET 요청보다는 보안성 측면에서 나음 => 그래도 암호화는 필요
    - Request Header에 포함되어서 데이터가 전달되기 때문에 전송하는 데이터의 길이 제한이 없음
    - HTML의 특수한 태그를 이용해서 서버 쪽 프로그램을 호출
    - 일반적으로는 GET 방식보다 POST 방식을 이용
