# TIL: DB 모델링과 SQL 기초
## 학습 날짜: 2025년 1월 14일
## 오늘 내가 새롭게 배운 것들
**정규화(Normalization)**
- 데이터베이스의 설계 단계에서 데이터 중복을 제거하고 데이터 무결성을 유지하기 위한 과정
- 데이터를 논리적으로 분리하여 효율적으로 저장하고 관리하도록 설계
- 데이터 중복 방지
- 데이터 일관성 및 무결성 유지
- 데이터 저장소의 효율성 향상
- 데이터 변경 시 발생할 수 있는 문제(삽입, 삭제, 갱신 이상) 최소화
- 제1정규형(1NF): 모든 속성은 반드시 하나의 값만 가지며, 유사한 속성이 반복되지 않는다.
- 제2정규형(2NF): 엔터티의 모든 일반 속성은 반드시 모든 주식별자에 종속되어야 한다.
- 제3정규형(3NF): 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다.

**반정규화(Denormalization)**
- 데이터베이스 설계에서 정규화를 통해 분리된 테이블을 특정 성능 향상이나 시스템 요구에 따라 다시 결합하거나 중복 데이터를 허용하는 과정
- 성능 최적화와 효율적인 데이터 액세스를 위해 데이터 모델을 조정하는 것
- 필요성
  - 정규화의 한계: 다수의 테이블 조인이 필요한 쿼리에서는 성능 저하 발생 가능, 읽기 속도가 중요한 상황에서는 비효율적
  - 성능 최적화: 쿼리 성능을 개선하기 위해 데이터를 중복 저장하거나 테이블을 합치는 방식으로 읽기 속도를 높임
  - OLAP(OnLine Analytical Processing) 시스템에서 활용
- 적용 방법
  - 속성 추가: 조인을 생략하고 직접 데이터를 조회 가능
  - 테이블 병합
  - 중복 데이터 저장: 데이터 분석 속도를 위해 요약 데이터를 미리 계산하고 테이블에 저장
  - 이력 데이터 관리: 트랜잭션 데이터 외에도 과거 데이터를 효율적으로 조회하기 위해 반정규화 적용
- 장점
  - 성능 개선: 읽기 속도 증가, 조인 연산 감소, 대규모 데이터 분석과 보고서 생성 시 유리
  - 간단한 쿼리: 데이터 구조 단순화로 인해 SQL 쿼리가 간결해지고 사용자가 테이블 구조를 쉽게 이해할 수 있음
  - 특정 요구사항 대응: 응용 프로그램에서 특정 데이터 집합을 자주 조회해야 하는 경우 적합
- 단점
  - 데이터 중복: 저장 공간 증가, 데이터 변경 시 모든 중복 데이터를 동기화해야 하는 복잡성 증가
  - 데이터 무결성 문제: 업데이트나 삭제 시 데이터 불일치 가능성이 높아짐
  - 유연성 감소: 테이블 병합이나 속성 추가로 인해 데이터 구조가 고정되어 유연성이 저하
 
**OLTP(OnLine Transaction Processing)**
- 실시간으로 다수의 트랜잭션을 처리하는 시스템
- 주로 운영 환경에서 사용
- 데이터의 입력, 저장, 수정 및 조회를 빠르게 처리
- 주요 특징
  - 실시간 처리: 다수의 사용자 요청을 신속하게 처리
  - 정규화된 데이터 구조: 데이터 중복 최소화 및 데이터 무결성 유지
  - 트랜잭션 관리: ACID(Atomicity, Consistency, Isolation, Durability) 속성을 보장
- 구성 요소
  - 데이터베이스: 관계형 데이터베이스
  - 애플리케이션: 웹 또는 모바일 기반 트랜잭션 처리 애플리케이션
  - 사용자: 실시간으로 데이터를 입력하거나 조회하는 최종 사용자

**OLAP(OnLine Analytical Processing)**
- 대규모 데이터를 분석하고, 요약 정보와 통찰력을 제공하는 시스템
- 데이터의 분석, 보고서 생성, 비즈니스 인사이트 제공
- 주요 특징
  - 복잡한 분석: 집계, 요약, 패턴 분석 쿼리를 통해 비즈니스 인사이트 제공
  - 대규모 데이터 처리: 데이터 웨어하우스 및 데이터 마트를 활용하여 분석
  - 다차원 데이터 분석: 시간, 지역, 제품 등 여러 차원을 기준으로 데이터 요약
- 구성 요소
  - 데이터 웨어하우스: 정형, 비정형 데이터를 통합하고 분석 중심으로 저장
  - OLAP 서버: 분석 쿼리를 실행하며 다차원 데이터 모델 제공
  - 사용자 인터페이스: 분석 결과를 시각적으로 제공하는 대시보드와 리포트
 
**OLTP와 OLAP의 상호작용**
- OLTP 시스템은 데이터 생성, OLAP 시스템은 데이터 활용
- OLTP에서 생성된 데이터를 OLAP로 추출하여 분석
- ETL 프로세스: 데이터 웨어하우스로 데이터를 전송하기 위해 ETL(Extract, Transform, Load) 사용
- OLTP -> ETL -> OLAP의 흐름

**논리 모델**
- 데이터베이스의 설계의 한 단계로 데이터의 구조와 관계를 기술하는 모델
- 특정 DBMS에 종속되지 않으며 데이터의 논리적 구조를 나타냄
- 엔터티(Entity)와 속성(Attribute) 정의
- 관계(Relationship)와 제약 조건 표현
- 데이터 중복 제거를 위한 정규화 적용

**물리 모델**
- 데이터베이스 설계의 최종 단계로 특정 DBMS에 종속적인 데이터 구조를 정의하는 모델
- 데이터의 저장소, 성능, 보안을 고려하여 실제 구현
- 데이터 타입과 제약 조건 지정
- 인덱스, 파티션, 저장소 구조 설계
- DBMS의 기술적 제약 및 성능 최적화 반영

**논리 모델 -> 물리 모델**
- 논리 모델에서 정의된 엔터티와 관계를 기반으로 물리적 테이블 생성
- 속성을 데이터 타입과 제약 조건으로 매핑
- 데이터베이스 성능을 고려하여 인덱스와 파티션 추가
- 변환 시 고려사항
  - 데이터베이스의 크기와 예상 트래픽
  - 읽기 및 쓰기 작업의 비율
  - DBMS의 기능 및 제약

**트랜잭션(Transaction)**
- 데이터베이스에서 하나의 논리적 작업 단위를 의미
- 특징(ACID)
  - 원자성(Atomicity)
    - 트랜잭션은 모두 실행되거나, 모두 실행되지 않아야 함
    - 실패 시 모든 변경 사항을 롤백하여 원래 상태로 복구
  - 일관성(Consistency)
    - 트랜잭션이 완료된 후 데이터베이스는 항상 일관된 상태로 유지해야 함
    - 데이터 무결성을 보장
  - 고립성(Isolation)
    - 여러 트랜잭션이 동시에 실행될 때 서로 간섭하지 않도록 독립적으로 처리
    - 트랜잭션 격리 수준으로 구현
  - 지속성(Durability)
    - 트랜잭션이 성공적으로 완료되면 변경된 데이터는 영구적으로 저장
    - 시스템 장애 후에도 데이터는 유지

**SQL(Structured Query Language)**
- 데이터베이스를 관리하고 조작하기 위한 언어
- 관계형 데이터베이스에서 데이터를 검색, 추가, 수정, 삭제(CRUD)하는 데 사용
- 데이터 정의, 조작, 제어를 위한 명령어 집합 제공
- 분류
  - DDL(Data Definition Language): 데이터베이스 구조 정의
  - DML(Data Manipulation Language): 데이터를 조작 (삽입, 수정, 삭제)
  - DCL(Data Control Language): 데이터 접근 권한 제어
  - TCL(Transaction Control Language): 트랜잭션 관리(Commit, Rollback)

**DDL(Data Definition Language)**
- 데이터베이스 객체를 정의하거나 변경하는 데 사용
- 주요 명령어
  - CREATE: 테이블, 뷰, 인덱스 생성
  - ALTER: 기존 객체 수정
    - 컬럼 추가: ADD
    - 컬럼 사이즈 or 데이터 타입 변경: MODIFY
    - 컬럼 삭제: DROP COLUMN
    - 컬럼명 변경: RENAME COLUMN
    - 테이블명 변경: RENAME
  - DROP: 객체 삭제
  - TRUNCATE: 테이블 데이터 초기화

**DML(Data Manipulation Language)**
- 데이터베이스의 데이터를 조작하는 데 사용
- 주요 명령어
  - SELECT: 데이터 검색
  - INSERT: 데이터 삽입
  - UPDATE: 데이터 수정
  - DELETE: 데이터 삭제 cf) TRUNCATE: 데이터를 공간까지 초기화시켜버림, WHERE절 사용 불가 관계(Relationship)와 제약 조건 표현
- 데이터 중복 제거를 위한 정규화 적용

**물리 모델**
- 데이터베이스 설계의 최종 단계로 특정 DBMS에 종속적인 데이터 구조를 정의하는 모델
- 데이터의 저장소, 성능, 보안을 고려하여 실제 구현
- 데이터 타입과 제약 조건 지정
- 인덱스, 파티션, 저장소 구조 설계
- DBMS의 기술적 제약 및 성능 최적화 반영

**논리 모델 -> 물리 모델**
- 논리 모델에서 정의된 엔터티와 관계를 기반으로 물리적 테이블 생성
- 속성을 데이터 타입과 제약 조건으로 매핑
- 데이터베이스 성능을 고려하여 인덱스와 파티션 추가
- 변환 시 고려사항
  - 데이터베이스의 크기와 예상 트래픽
  - 읽기 및 쓰기 작업의 비율
  - DBMS의 기능 및 제약

**트랜잭션(Transaction)**
- 데이터베이스에서 하나의 논리적 작업 단위를 의미
- 특징(ACID)
  - 원자성(Atomicity)
    - 트랜잭션은 모두 실행되거나, 모두 실행되지 않아야 함
    - 실패 시 모든 변경 사항을 롤백하여 원래 상태로 복구
  - 일관성(Consistency)
    - 트랜잭션이 완료된 후 데이터베이스는 항상 일관된 상태로 유지해야 함
    - 데이터 무결성을 보장
  - 고립성(Isolation)
    - 여러 트랜잭션이 동시에 실행될 때 서로 간섭하지 않도록 독립적으로 처리
    - 트랜잭션 격리 수준으로 구현
  - 지속성(Durability)
    - 트랜잭션이 성공적으로 완료되면 변경된 데이터는 영구적으로 저장
    - 시스템 장애 후에도 데이터는 유지

**SQL(Structured Query Language)**
- 데이터베이스를 관리하고 조작하기 위한 언어
- 관계형 데이터베이스에서 데이터를 검색, 추가, 수정, 삭제(CRUD)하는 데 사용
- 데이터 정의, 조작, 제어를 위한 명령어 집합 제공
- 분류
  - DDL(Data Definition Language): 데이터베이스 구조 정의
  - DML(Data Manipulation Language): 데이터를 조작 (삽입, 수정, 삭제)
  - DCL(Data Control Language): 데이터 접근 권한 제어
  - TCL(Transaction Control Language): 트랜잭션 관리(Commit, Rollback)

**DDL(Data Definition Language)**
- 데이터베이스 객체를 정의하거나 변경하는 데 사용
- 주요 명령어
  - CREATE: 테이블, 뷰, 인덱스 생성
  - ALTER: 기존 객체 수정
    - 컬럼 추가: ADD
    - 컬럼 사이즈 or 데이터 타입 변경: MODIFY
    - 컬럼 삭제: DROP COLUMN
    - 컬럼명 변경: RENAME COLUMN
    - 테이블명 변경: RENAME
  - DROP: 객체 삭제
  - TRUNCATE: 테이블 데이터 초기화

**DML(Data Manipulation Language)**
- 데이터베이스의 데이터를 조작하는 데 사용
- 주요 명령어
  - SELECT: 데이터 검색
  - INSERT: 데이터 삽입
  - UPDATE: 데이터 수정
  - DELETE: 데이터 삭제 cf) TRUNCATE: 데이터를 공간까지 초기화시켜버림, WHERE절 사용 불가
 
**DCL(Data Control Language)**
- 데이터베이스 접근 권한을 관리
- 주요 명령어
  - GRANT: 권한 부여
  - REVOKE: 권한 회수
 
**TCL(Transaction Control Language)**
- 트랜잭션 관리를 위한 명령어 제공
- 주요 명령어
  - COMMIT: 트랜잭션 확정
  - ROLLBACK: 트랜잭션 취소
  - SAVEPOINT: 특정 지점 저장

**SELECT**
- SQL에서 데이터를 검색하는 명령어
- 테이블에서 원하는 데이터를 추출
- 조건, 정렬, 그룹핑 등으 ㄹ사용하여 복잡한 질의 가능
- 기본 문법: SELECT 컬럼명 FROM 테이블명;

**ALIAS**
- SQL 쿼리에서 컬럼이나 테이블에 부여하는 임시 이름
- 결과 집합의 가독성을 높이고 복잡한 쿼리를 간결하게 작성할 수 있도록 돕는 기능
- 데이터베이스 구조에 영향을 주지 않음
- 컬럼 ALIAS
  - 연산 결과에 의미를 부여
  - 출력 결과의 컬럼 이름을 변경하여 가독성을 높임
  - AS 생략 가능
- 테이블 ALIAS
  - 테이블 이름이 길거나 복잡할 때 간결한 이름으로 대체
  - JOIN 쿼리에서 테이블 이름을 축약하여 사용
 
**NULL**
- "값이 없음"을 의미
- 숫자, 문자 등 어떤 데이터도 저장되지 않은 상태
- 데이터가 "비어 있다"고 간주되며, 이는 0이나 빈 문자열("")과는 다름
- 정의되지 않은 상태
- 다른 값과 비교할 수 없음
- 비교 연산을 수행하면 항상 FALSE

**WHERE**
- WHERE 절로 조건을 지정하여 데이터를 필터링
- SELECT, UPDATE, DELETE와 함께 사용 데이터베이스 객체를 정의하거나 변경하는 데 사용
- 주요 명령어
  - CREATE: 테이블, 뷰, 인덱스 생성
  - ALTER: 기존 객체 수정
    - 컬럼 추가: ADD
    - 컬럼 사이즈 or 데이터 타입 변경: MODIFY
    - 컬럼 삭제: DROP COLUMN
    - 컬럼명 변경: RENAME COLUMN
    - 테이블명 변경: RENAME
  - DROP: 객체 삭제
  - TRUNCATE: 테이블 데이터 초기화

**DML(Data Manipulation Language)**
- 데이터베이스의 데이터를 조작하는 데 사용
- 주요 명령어
  - SELECT: 데이터 검색
  - INSERT: 데이터 삽입
  - UPDATE: 데이터 수정
  - DELETE: 데이터 삭제 cf) TRUNCATE: 데이터를 공간까지 초기화시켜버림, WHERE절 사용 불가
 
**DCL(Data Control Language)**
- 데이터베이스 접근 권한을 관리
- 주요 명령어
  - GRANT: 권한 부여
  - REVOKE: 권한 회수
 
**TCL(Transaction Control Language)**
- 트랜잭션 관리를 위한 명령어 제공
- 주요 명령어
  - COMMIT: 트랜잭션 확정
  - ROLLBACK: 트랜잭션 취소
  - SAVEPOINT: 특정 지점 저장

**SELECT**
- SQL에서 데이터를 검색하는 명령어
- 테이블에서 원하는 데이터를 추출
- 조건, 정렬, 그룹핑 등으 ㄹ사용하여 복잡한 질의 가능
- 기본 문법: SELECT 컬럼명 FROM 테이블명;

**ALIAS**
- SQL 쿼리에서 컬럼이나 테이블에 부여하는 임시 이름
- 결과 집합의 가독성을 높이고 복잡한 쿼리를 간결하게 작성할 수 있도록 돕는 기능
- 데이터베이스 구조에 영향을 주지 않음
- 컬럼 ALIAS
  - 연산 결과에 의미를 부여
  - 출력 결과의 컬럼 이름을 변경하여 가독성을 높임
  - AS 생략 가능
- 테이블 ALIAS
  - 테이블 이름이 길거나 복잡할 때 간결한 이름으로 대체
  - JOIN 쿼리에서 테이블 이름을 축약하여 사용
 
**NULL**
- "값이 없음"을 의미
- 숫자, 문자 등 어떤 데이터도 저장되지 않은 상태
- 데이터가 "비어 있다"고 간주되며, 이는 0이나 빈 문자열("")과는 다름
- 정의되지 않은 상태
- 다른 값과 비교할 수 없음
- 비교 연산을 수행하면 항상 FALSE
- 기본 연산에서는 항상 NULL
- 집계 함수에서는 연산에서 제외

**WHERE**
- WHERE 절로 조건을 지정하여 데이터를 필터링
- SELECT, UPDATE, DELETE와 함께 사용
- 다양한 연산자를 활용하여 복잡한 조건 설정 가능

**연산자**
- 비교 연산자
  - =: 값이 같은지 비교
  - <>: 값이 다른지 비교
  - <,>: 값이 작거나 큰지 비교
  - <=,>=: 값이 작거나 같음 혹은 크거나 같음을 비교
- 논리 연산자
  - AND: 조건이 모두 TRUE인 경우만 결과 반환
  - OR: 여러 조건 중 하나만 만족하면 결과 반환
  - NOT: 조건의 결과를 반전시킴
- 특수 연산자
  - LIKE: 특정 패턴을 가진 데이터를 검색
    - %: 0개 이상의 문자를 대체
    - _: 1개의 문자를 대체
    - ESCAPE: ESCAPE로 지정한 문자열 뒤의 와일드카드는 일반 문자로 취
  - IN: 지정된 값 중 하나와 일치하는 데이터를 찾음, OR 조건으로 대체 가능
  - BETWEEN: 지정된 범위 내에 있는 값을 찾음, 시작 값과 끝 값을 포함
  - IS NULL: NULL 값을 가진 데이터를 찾음
    - IS NOT NULL: NULL 값이 아닌 데이터를 찾음
