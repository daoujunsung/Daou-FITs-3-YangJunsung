# TIL: 금융 시스템의 이해와 SQL 기초
## 학습 날짜: 2025년 1월 15일
## 오늘 내가 새롭게 배운 것들
```
-- 1. 직원과 부서 정보 조회
-- 직원 이름과 소속 부서의 근무지를 조회하세요
SELECT E.NAME, D.LOCATION
FROM EMPLOYEES E
    JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;

-- 2. 직원과 매니저 정보 조회
-- 직원 이름과 해당 직원의 매니저 이름을 조회하세요. (단, 매니저가 없는 직원도 출력)
SELECT E1.NAME EMPLOYEE,
    E2.NAME MANAGER
FROM EMPLOYEES E1
    LEFT OUTER JOIN EMPLOYEES E2
    ON E1.MANAGER_ID = E2.EMPLOYEE_ID;
    
-- 3. 직원과 급여 정보 조회
-- 직원 이름과 급여를 조회하되 급여가 NULL인 경우 "N/A"로 출력하세요.
SELECT NAME, 
    NVL(TO_CHAR(SALARY), 'N/A') SALARY 
FROM EMPLOYEES;

-- 4. 특정 부서 직원 조회
-- 근무지가 Chicago인 직원 이름과 급여를 조회하세요
SELECT E.NAME, E.SALARY
FROM EMPLOYEES E
    JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
WHERE D.LOCATION = 'Chicago';

-- 5. 특정 급여 범위 조회
-- 급여가 5000 이상 10000 이하인 직원의 이름과 급여, 부서명을 조회하세요.
SELECT E.NAME, E.SALARY, D.DEPARTMENT_NAME
FROM EMPLOYEES E
    JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
WHERE E.SALARY BETWEEN 5000 AND 10000;

-- 6. 최근 3개월 내 입사한 직원
-- 최근 3개월 이내 입사한 직원 이름과 입사일을 조회하세요
SELECT NAME, HIRE_DATE
FROM EMPLOYEES
WHERE MONTHS_BETWEEN(SYSDATE, HIRE_DATE) <= 3;

-- 7. 부서장 이름 조회
-- 각 부서의 부서장 이름과 부서 이름을 조회하세요.
SELECT E.NAME, D.DEPARTMENT_NAME
FROM DEPARTMENTS D
    LEFT OUTER JOIN EMPLOYEES E
        ON D.MANAGER_ID = E.EMPLOYEE_ID;
        
-- 8. 직무별 직원 수 조회
-- 직무별 직원 수를 출력하세요.
SELECT JOB_ID "직무", 
    COUNT(*) "직원 수"
FROM EMPLOYEES
GROUP BY JOB_ID;

-- 9. 부서별 평균 급여
-- 부서명과 부서별 평균 급여를 조회하세요.(단, 평균 급여는 올림하여 출력)
SELECT D.DEPARTMENT_NAME "부서명",
    CEIL(AVG(E.SALARY)) "평균 급여"
FROM DEPARTMENTS D
    JOIN EMPLOYEES E
        ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
GROUP BY D.DEPARTMENT_NAME;

-- 10. 직원의 총 급여 합계
-- 직원의 총 급여 합계를 조회하세요.
SELECT SUM(SALARY) "총 급여" FROM EMPLOYEES;

-- 11. 대출 정보와 고객 이름 조회
-- 대출 금액과 고객 이름을 대출 금액이 높은 사람부터 조회하세요
SELECT L.AMOUNT, C.NAME
FROM LOANS L
    RIGHT OUTER JOIN CUSTOMERS C
    ON L.CUSTOMER_ID = C.CUSTOMER_ID
ORDER BY L.AMOUNT DESC NULLS LAST;

-- 12. 대출 상태별 고객 수
-- 대출 상태별 고객 수를 조회하세요.
SELECT STATUS, COUNT(DISTINCT CUSTOMER_ID) "고객 수"
FROM LOANS
GROUP BY STATUS;

-- 13. 직원 수가 가장 적은 부서
-- 직원 수가 가장 적은 부서를 조회하세요.
SELECT *
FROM (
    SELECT DEPARTMENT_ID, COUNT(*) COUNT
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID IS NOT NULL
    GROUP BY DEPARTMENT_ID
    ORDER BY COUNT(*)
)
WHERE ROWNUM = 1;

-- 14. 월별 신규 고객 수
-- 월별 신규 고객 수를 최근 순으로 조회하세요
SELECT TO_CHAR(CREATED_AT, 'YYYY-MM') "월",
    COUNT(*)
FROM CUSTOMERS
GROUP BY TO_CHAR(CREATED_AT, 'YYYY-MM')
ORDER BY "월" DESC;

-- 15. 상품 카테고리 계층 구조 조회
-- 상품 카테고리와 상위 카테고리를 조회하세요.(단, 상위 카테고리가 없는 데이터도 출력)
SELECT SC1.CATEGORY_NAME "상품 카테고리", 
    SC2.CATEGORY_NAME "상위 카테고리"
FROM SHOPPING_CATEGORIES SC1
    LEFT OUTER JOIN SHOPPING_CATEGORIES SC2
    ON SC1.PARENT_CATEGORY_ID = SC2.CATEGORY_ID;

-- 16. 특정 지점의 대출 합계
-- 'Branch 8' 지점의 대출 총합을 조회하세요. (단, 소수점 아래는 버림)
SELECT FLOOR(SUM(L.AMOUNT)) "대출 총합"
FROM LOANS L
    JOIN BRANCHES B
    ON L.BRANCH_ID = B.BRANCH_ID
WHERE B.NAME = 'Branch 8';

-- 17. 직무별 직원의 평균 급여와 급여 등급 조회
-- 직무별 직원의 평균 급여를 계산하여 평균 급여가 5000 이상인 경우만 출력합니다. 
-- 평균 급여가 8000 이상인 경우 "HIGH", 6000 이상 8000 미만인 경우 "MEDIUM", 6000 미만인 경우 "LOW"로 표시합니다.
-- 결과는 평균 급여가 높은 순으로 정렬하세요.
SELECT JOB_ID "직무",
    ROUND(AVG(SALARY),2) "평균 급여",
    CASE
        WHEN AVG(SALARY) >= 8000 THEN 'HIGH'
        WHEN AVG(SALARY) >= 6000 THEN 'MEDIUM'
        ELSE 'LOW'
    END "급여 등급"
FROM EMPLOYEES
GROUP BY JOB_ID
HAVING AVG(SALARY) >= 5000
ORDER BY "평균 급여" DESC;

-- 18. 고객별 거래 내역
-- 고객 이름과 거래 내역(금액)을 조회하여 고객 이름으로 오름차순 하세요.
SELECT C.NAME "고객명", SUM(T.AMOUNT) "거래 금액"
FROM CUSTOMERS C
    JOIN ACCOUNTS A
    ON C.CUSTOMER_ID = A.CUSTOMER_ID
    JOIN TRANSACTIONS T
    ON A.ACCOUNT_ID = T.ACCOUNT_ID
GROUP BY C.NAME
ORDER BY TO_NUMBER(SUBSTR(C.NAME, 10, LENGTH(C.NAME)-9));

-- 19. 부서별 직원의 평균 입사일 차이
-- 부서별로 평균 근속일을 계산하여 부서 이름으로 오름차순 정렬하세요. (단, 평균 근속일은 반올림하여 정수로 표현)
SELECT D.DEPARTMENT_NAME "부서명",
    ROUND(AVG(SYSDATE - E.HIRE_DATE)) "평균 근속일"
FROM EMPLOYEES E
    JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
GROUP BY D.DEPARTMENT_NAME
ORDER BY TO_NUMBER(REPLACE(D.DEPARTMENT_NAME, 'Department ', ''));

-- 20. 대출 금액 상위 5명 고객 조회
-- 대출 금액이 가장 높은 상위 5명의 고객의 이름과 대출 금액을 조회하세요. (단, 대출 금액이 동일한 경우 고객의 이름으로 오름차순)
SELECT *
FROM (
    SELECT C.NAME "고객명", SUM(L.AMOUNT) "대출 금액"
    FROM LOANS L
        JOIN CUSTOMERS C
        ON C.CUSTOMER_ID = L.CUSTOMER_ID
    GROUP BY C.NAME
    ORDER BY "대출 금액" DESC, "고객명"
)
WHERE ROWNUM <= 5;
```
