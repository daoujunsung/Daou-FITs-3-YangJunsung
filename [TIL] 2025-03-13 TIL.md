# TIL
## 학습 날짜: 2025년 3월 13일
## 어떤 문제가 있었는지
- 이차원 배열에서 특정 행을 삭제할 수 없음
- 함수 선언을 할 때 이차원 배열을 인수로 받아오지 못함
## 그 문제를 해결해보기 위해 내가 시도해본 것들
- 이차원 배열을 출력할 때 삭제를 원하는 행만 출력하지 않도록 조정
- 이차원 배열을 인수로 받아오는 방법을 검색해봄
## 어떻게 그 문제를 해결했는지
- 이차원 배열을 삭제하는 행부터 한 행씩 당겨오고 마지막 줄을 출력하지 않도록 수정
- 이차원 배열의 크기를 정적으로 지정해주는 방법으로 해결
## 오늘 내가 새롭게 배운 것들
**((void (*)(int **, int, int)) operations[choice](ptr_array, ROWS, COLS);**
- 함수 포인터 배열
  - operations 함수 포인터 배열에는 여러 함수 포인터가 저장
- 배열의 인덱스를 통해 operations에서 특정 함수를 선택하여 실행
- (void (*)(int **, int, int) 형태로 명시적 형변환을 해줌
- 이 형변환은 operations[choice]가 실제로 int** 타입을 받는 함수 포인터임을 컴파일러에게 알려주기 위함
