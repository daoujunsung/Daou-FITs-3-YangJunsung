# TIL: Session, JSP
## 학습 날짜: 2025년 2월 18일
## 오늘 내가 새롭게 배운 것들
**Session**
- HTTP 프로토콜은 Stateless Protocol이기 때문에 서버는 기본적으로 클라이언트를 구별할 수 없음
- Cookie
  - 문자열 => 처리가 힘들다
  - 데이터(ex.name=홍길동;age=20) + 유효 기간 + 유효 도메인
  - Servlet에서 doGet()이나 doPost()가 실행될 때 쿠키를 생성해서 response 객체를 통해 설정
  - Client Web Browser에 저장 => 보안성 없음
  - 길이 제한이 있어서 많은 양의 데이터를 저장할 수 없음
  - 브라우저 자체적으로도 쿠키로 저장할 수 있는 데이터를 제한해둠
- Session
  - HttpSession class의 인스턴스
  - WAS에 저장
  - 쿠키에 SessionID를 담아 전달하는 방식으로 이용
  - 한 클라이언트당 하나의 세션이 할당
  - req.getSession(): 세션을 할당하는 메서드
  - setAttribute(): 세션에 값을 저장하는 메서드
  - getAttribute(): 세션에 저장된 값을 가져오는 메서드

**JSP**
- View 역할을 하는 Servlet
- HTML 안에 Java 코드를 넣어서 표현
- 출력 HTML을 쉽게 작성하기 위한 용도
