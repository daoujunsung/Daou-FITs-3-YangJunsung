# TIL
## 학습 날짜: 2025년 3월 28일
## 어떤 문제가 있었는지
- 메모리 부족 문제
- 월별 수익률과 월별 누적 수익률 계산에서 에러 발생
## 그 문제를 해결해보기 위해 내가 시도해본 것들
- free() 위치를 이리저리 옮겨봄
- calloc()을 이용하여 0으로 값을 모두 초기화해줌
- 이차원 배열들을 여러 개 추가하여 직관적으로 코드 변경
## 어떻게 그 문제를 해결했는지
- 메모리가 부족한 게 아니라 크기 자체가 잘못 선언되어있던 것. 크기 다시 선언해줘서 해결
- calloc()으로 0으로 값을 초기화하고 만든 이차원 배열들에서 맞는 위치의 값을 적절히 불러와 누적해서 더해줌
