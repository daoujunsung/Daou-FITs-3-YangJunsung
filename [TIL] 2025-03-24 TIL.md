# TIL: C언어 전처리기, 모듈화
## 학습 날짜: 2025년 3월 24일
## 오늘 내가 새롭게 배운 것들
**C언어 전처리기**
- 컴파일 전에 소스 코드에 특정 작업을 수행하는 기능
- 유지보수성을 높이고 중복을 줄이며 효율적인 컴파일 가능
- #include
  - 반복되는 코드를 제거할 때 이용
  - 함수 및 구조체를 헤더 파일로 분리
  - 코드의 재사용과 유지보수성 향상
  - 여러 파일에서 공통적으로 사용되는 헤더 파일을 쉽게 포함 가능
  - 코드가 모듈화되어 유지보수가 쉬어짐
- #define
  - 코드 간소화 및 가독성을 향상
  - 매크로를 사용하여 상수 및 함수를 간결하게 정의
  - 상수를 정의하여 의미 있는 코드 작성 가능
  - 매크로 함수로 반복되는 연산을 줄일 수 있음
- #ifdef, #ifndef, #if
  - 특정 조건에 따라 코드 실행을 다르게 설정
  - 디버깅 코드 관리
  - 운영체제 또는 환경에 따른 코드 분기 처리
- #ifndef, #define, #endif
  - 헤더 파일이 중복 포함되는 문제를 방지
  - 코드 안정성을 확보
- #pragma
  - 컴파일러 지시
  - 특정 컴파일러 설정을 변경 가능
  - 불필요한 경고 무시
- #error
  - 특정 조건에서 컴파일 중단
  - 잘못된 환경에서 빌드를 막음
  - 사용자가 특정 설정을 강제하도록 유

**모듈화(Modularization)**
- 프로그램을 독립적인 기능을 수행하는 작은 단위(모듈)로 나누어 개발하는 소프트웨어 설계 기법
- 모듈은 각각의 기능을 담당하며 서로 독립적으로 작성, 수정, 테스트할 수 있어 유지보수성과 재사용성 증가
- 논리적으로 같은 등급의 소스 파일을 계속해서 분할하는 것
- 소스파일에서 혹은 어떤 함수에서 일어나는 일의 책임을 그 소스 파일이나 함수가 지도록 하는 것
- C언어에서의 모듈화
  - 헤더 파일과 소스 파일을 분리하여 모듈화 구현
    - 헤더 파일(.h): 함수 선언 및 전역 변수를 정의하는 부분
    - 소스 파일(.c): 함수를 실제로 구현하는 부분
    - 함수 선언(인터페이스)와 구현을 나누어 관리하는 것
  - 독립적인 코드 관리 기능
    - 필요할 때 필요한 모듈만 수정
    - 한 모듈을 다른 프로젝트에서도 재사용 가능
  - 컴파일 시간 단축 및 협업 용이
    - 여러 개발자가 각각 모듈을 동시에 개발 가능
    - 변경된 파일만 다시 컴파일하면 되므로 빌드 속도 개선
- 모듈화의 기준
  - 하나의 모듈은 한 가지 역할만 수행
  - 인터페이스와 구현 분리, 헤더 파일과 소스 파일 분리
  - 높은 응집도와 낮은 결합도, 관련 기능끼리 묶고 모듈 간 의존성 최소화
  - 재사용성 고려 -> 공통 기능은 별도의 모듈로 분리
  - 변경 가능성 고려 -> 확장 가능하도록 설계하여 유지보수를 쉽게 함
- 모듈화하는 방법
  - 헤더 파일(.h)과 소스 파일(.c) 분리
    - 헤더 파일: 함수 선언, 매크로, typedef
    - 소스 파일: 함수 정의 및 내부 로직 구현
  - 각 모듈마다 역할을 명확히 분리
    - main.c: 프로그램의 진입점 역할
    - bank.h, bank.c: 시스템의 핵심 로직 역할
    - db.h, db.c: 데이터베이스 연동 역할
    - ui.h, ui.c: 화면에 관련된 역할
  - 헤더 가드 사용하기: #ifndef ~ #define ~ #endif
  - 전역 변수는 extern으로 선언하여 여러 파일에서 공유
  - static을 사용하여 내부에서만 사용되는 함수 숨기기(외부에서 접근 불가능한 변수를 사용해서 독립적으로 모듈화 가능)
- 전역 static 변수
  - 같은 이름의 변수를 다른 파일에서 중복 선언 가능
  - count, num, data와 같은 이름은 흔히 여러 함수나 파일에서 사용할 수 있기 때문에 static 변수로 선언하여 오류를 줄일 수 있음
- 소스 파일의 분할 규칙
  - main은 되도록 다른 함수, 구조체(정의 포함), 변수를 포함하지 않는다.
  - 각각의 역할 별로 소스 파일을 분할
  - 대부분 다른 소스 파일에서의 전역변수를 직접 끌어와 쓰지 않는다(extern으로 끌어오지 않는다)
  - 각 소스 파일은 가능한 독립적이게 만듦
  - 한 소스 파일이 다른 소스 파일에 의존적이게 되는 경우는 상관없지만 되도록 피한다
  - 각 소스 파일의 전역변수들은 특별한 경우가 아니라면 무조건 static을 걸어준다.
