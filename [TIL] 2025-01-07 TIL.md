# TIL: 알고리즘과 자료구조
## 학습 날짜: 2025년 1월 7일
## 오늘 내가 새롭게 배운 것들
**알고리즘(Algorithm)**
: 어떤 문제를 해결하기 위한 일련의 절차
- 0개 이상의 입력과 1개 이상의 출력
- 반드시 종료되어야 함
- 모든 명령이 실행 가능해야 함   

**시간 복잡도(Time Complexity)**   
: 알고리즘이 얼마나 효율적으로 동작하는지 평가할 수 있는 중요한 척도, 입력 크기(n)에 대해 실행되는 시간을 수학적으로 표현
- 점근적 표기법(Asymptotic Notation): 시간 복잡도를 근사치로 표현한 것, 오메가, 세타, 빅오 표기법이 있음
- 오메가 표기법(Big-Ω Notation): 최선의 경우에서의 시간 복잡도
- 세타 표기법(Big-Θ Notation): 평균적인 경우에서의 시간 복잡도
- 빅오 표기법(Big-O Notation): 최악의 경우에서의 시간 복잡도, 일반적으로 사용
- $$O(1)$$: 상수, 입력에 관계없이 복잡도는 동일하게 유지
- $$O(n)$$: 선형, 입력이 증가하면 처리시간 또는 메모리 사용이 선형적으로 증가
- $$O(n^2)$$: 반복문이 두 번 있는 케이스
- $$O(logn)$$, $$O(nlogn)$$: 문제를 해결하기 위해 필요한 단계들이 연산마다 특정 요인에 의해 줄어듦

**자료구조**
: 프로그램에서 쉽게 이용할 수 있도록 구성된 데이터 간의 논리적인 관계
- 배열(Array)
- 연결 리스트(Linked List)
- 스택(Stack)
- 큐(Queue)
- 그래프(Graph)
- 트리(Tree)

**배열(Array)**
- 같은 자료형의 데이터를 순서대로 나열한 구조
- 각 요소는 인덱스를 통해 직접 접근
- 고정된 크기를 가진 연속된 메모리 공간에 데이터를 저장

**연결 리스트(Linked List)**
- 각 데이터들을 포인터로 연결하여 관리하는 구조
- 데이터 영역에 여러 개의 데이터가 저장될 수 있음
- 요소들이 메모리 상에서 연속적이지 않으며, 각각의 노드는 동적으로 할당되어 연결
- 단순 연결 리스트(Singly Linked List): 노드에 포인터 영역이 하나인 연결 리스트
- 이중 연결 리스트(Doubly Linked List): 각 노드에 다음 노드를 가리키는 포인터 영역과 이전 노드를 가리키는 포인터 영역을 포함

**스택(Stack)**
- 데이터의 삽입과 삭제가 한쪽 방향에서만 일어나는 구조
- 후입선출(LIFO, Last-In First-Out)
- 스택 삽입(Push): 스택의 상단에 데이터를 삽입
- 스택 삭제(Pop): 스택의 최상단의 데이터를 삭제, 삭제한 데이터는 반환될 수 있음
- 파이썬으로 구현한 스택
```
stack=[]
while True:
    select=int(input('1:삽입, 2:삭제, 3:종료 '))
    if select==1:
        data=int(input('삽입할 데이터: '))
        stack.append(data)
        print(stack)
    elif select==2:
        if len(stack)==0:
            print('스택이 비었습니다.')
        else:
            data=stack.pop()
            print('삭제된 데이터:', data)
            print(stack)
    else:
        break
```

**큐(Queue)**
- 데이터가 한쪽 방향으로 삽입되고 반대 방향으로 삭제되는 구조
- 선입선출(FIFO, First-In First_Out)
- 큐 삽입(Enqueue): 큐의 뒤쪽에 데이터를 삽입
- 큐 삭제(Dequeue): 큐의 가장 앞쪽의 데이터를 제거
- 파이썬으로 구현한 큐
```
queue=[]
while True:
    select=int(input('1:삽입, 2:삭제, 3:종료 '))
    if select==1:
        data=int(input('삽입할 데이터: '))
        queue.append(data)
        print(queue)
    elif select==2:
        if len(queue)==0:
            print('큐가 비었습니다.')
        else:
            data=queue.pop(0)
            print('삭제된 데이터:', data)
            print(queue)
    else:
        break
```

**우선순위 큐(Priority Queue)**
- 각 요소가 우선순위를 가지며, 큐에서 데이터를 꺼낼 때 우선순위가 높은 요소부터 꺼내는 자료 구조
- 힙(Heap): 최대힙, 최소힙을 사용하면 삽입과 삭제 연산 모두 효율적으로 처리할 수 있는 우선순위 큐 구현 가능
- 최대힙 예시
```
# 최대힙
import heapq

# 빈 우선순위 큐 생성
pq = []

# 최대 힙처럼 사용하기 위해 우선순위에 -1을 곱해 삽입
heapq.heappush(pq, (-2, "task 2"))
heapq.heappush(pq, (-1, "task 1"))
heapq.heappush(pq, (-3, "task 3")) #우선순위가 가장 큼

# 우선순위가 가장 큰 요소 꺼내기 (최대 힙처럼 작동)
priority, task = heapq.heappop(pq)
print(f"Task with priority {-priority}: {task}")
```
- 최소힙 예시
```
# 최소힙
import heapq

# 빈 우선순위 큐 생성
pq = []

# 요소 삽입 (우선순위, 데이터 형식으로 삽입)
heapq.heappush(pq, (2, "task 2"))
heapq.heappush(pq, (1, "task 1")) #우선순위가 가장 작음
heapq.heappush(pq, (3, "task 3"))

# 우선순위가 가장 작은 요소 꺼내기 (최소 힙이므로 가장 작은 우선순위가 먼저 나옵니다)
priority, task = heapq.heappop(pq)
print(f"Task with priority {priority}: {task}")
```

**그래프(Graph)**
- 정점과 간선으로 구성된 자료구조, 서로 연결된 관계를 표현
- 정점(Node/Vertex): 그래프의 지점들
- 간선(Edge): 정점과 정점을 연결하는 선
- 그래프의 표현: (정점의 집합, 간선의 집합)으로 표현, $$G=(V,E)$$
- 무방향 그래프(Undirected Graph): 방향성이 없는 간선으로 이루어진 그래프
- 방향 그래프(Directed Graph): 방향성이 있는 간선으로 이루어진 그래프
- 완전 그래프(Complete Graph): 모든 정점 사이에 간선이 있는 그래프
- 가중 그래프(Weighted Graph): 간선마다 가중치를 부여한 그래프
- 다중 그래프(Multigraph): 두 정점을 연걸하는 간선이 여러 개인 다중 간선이 존재하는 그래프
- 루프(Loop): 자기 자신을 연결한 간선
- 유사 그래프(Pseudograph): 다중 간선과 루프를 포함한 그래프
- 인접 또는 이웃(Adjacent or Neighbor): 무방향 그래프에서 간선으로 연결된 두 정점
- 차수(Degree): 정점에 접한 간선의 수
- 인접 리스트(Adjacency List): 인접한 정점들의 리스트
- 인접 행렬(Adjacency Matrix): i와 j 사이의 간선의 개수를 i행 j열썬으로 구현한 큐
```
queue=[]
while True:
    select=int(input('1:삽입, 2:삭제, 3:종료 '))
    if select==1:
        data=int(input('삽입할 데이터: '))
        queue.append(data)
        print(queue)
    elif select==2:
        if len(queue)==0:
            print('큐가 비었습니다.')
        else:
            data=queue.pop(0)
            print('삭제된 데이터:', data)
            print(queue)
    else:
        break
```

**우선순위 큐(Priority Queue)**
- 각 요소가 우선순위를 가지며, 큐에서 데이터를 꺼낼 때 우선순위가 높은 요소부터 꺼내는 자료 구조
- 힙(Heap): 최대힙, 최소힙을 사용하면 삽입과 삭제 연산 모두 효율적으로 처리할 수 있는 우선순위 큐 구현 가능
- 최대힙 예시
```
# 최대힙
import heapq

# 빈 우선순위 큐 생성
pq = []

# 최대 힙처럼 사용하기 위해 우선순위에 -1을 곱해 삽입
heapq.heappush(pq, (-2, "task 2"))
heapq.heappush(pq, (-1, "task 1"))
heapq.heappush(pq, (-3, "task 3")) #우선순위가 가장 큼

# 우선순위가 가장 큰 요소 꺼내기 (최대 힙처럼 작동)
priority, task = heapq.heappop(pq)
print(f"Task with priority {-priority}: {task}")
```
- 최소힙 예시
```
# 최소힙
import heapq

# 빈 우선순위 큐 생성
pq = []

# 요소 삽입 (우선순위, 데이터 형식으로 삽입)
heapq.heappush(pq, (2, "task 2"))
heapq.heappush(pq, (1, "task 1")) #우선순위가 가장 작음
heapq.heappush(pq, (3, "task 3"))

# 우선순위가 가장 작은 요소 꺼내기 (최소 힙이므로 가장 작은 우선순위가 먼저 나옵니다)
priority, task = heapq.heappop(pq)
print(f"Task with priority {priority}: {task}")
```

**그래프(Graph)**
- 정점과 간선으로 구성된 자료구조, 서로 연결된 관계를 표현
- 정점(Node/Vertex): 그래프의 지점들
- 간선(Edge): 정점과 정점을 연결하는 선
- 그래프의 표현: (정점의 집합, 간선의 집합)으로 표현, $$G=(V,E)$$
- 무방향 그래프(Undirected Graph): 방향성이 없는 간선으로 이루어진 그래프
- 방향 그래프(Directed Graph): 방향성이 있는 간선으로 이루어진 그래프
- 완전 그래프(Complete Graph): 모든 정점 사이에 간선이 있는 그래프
- 가중 그래프(Weighted Graph): 간선마다 가중치를 부여한 그래프
- 다중 그래프(Multigraph): 두 정점을 연걸하는 간선이 여러 개인 다중 간선이 존재하는 그래프
- 루프(Loop): 자기 자신을 연결한 간선
- 유사 그래프(Pseudograph): 다중 간선과 루프를 포함한 그래프
- 인접 또는 이웃(Adjacent or Neighbor): 무방향 그래프에서 간선으로 연결된 두 정점
- 차수(Degree): 정점에 접한 간선의 수
- 인접 리스트(Adjacency List): 인접한 정점들의 리스트
- 인접 행렬(Adjacency Matrix): i와 j 사이의 간선의 개수를 i행 j열에 표현한 행렬
- 근접 행렬(Incidence Maxtrix): 정점과 간선 사이에 존재를 나타낸 부울 행렬
- 경로(Path): 간선을 연결하는 정점들을 나열한 것
- 깊이 우선 탐색(DFS, Depth-First Search): 진행하다가 더 이상 진행할 수 없으면 왔던 길을 되돌아가면서 아직 방문하지 않은 정점을 방문
- 재귀 방식으로 구현한 DFS
```
def dfs(graph, v, visited):
    # 현재 정점을 방문
    visited[v] = True
    print(v, end=" ")

    # 현재 정점과 인접한 정점들 중 방문하지 않은 정점 방문
    for neighbor in graph[v]:
        if not visited[neighbor]:
            dfs(graph, neighbor, visited)

# 그래프와 호출 예시
graph = {
    0: [1, 2],
    1: [0, 3, 4],
    2: [0],
    3: [1],
    4: [1]
}
visited = [False] * 5  # 5개의 정점에 대해 방문 여부 기록
dfs(graph, 0, visited)
```
- 스택을 이용한 반복 방식으로 구현한 DFS
```
def dfs(graph, start):
    visited = [False] * len(graph)  # 방문 여부 기록
    stack = [start]  # 시작 정점은 스택에 넣고 시작

    while stack:
        v = stack.pop()  # 스택에서 정점을 꺼냄
        if not visited[v]:  # 방문하지 않은 정점만 방문
            visited[v] = True
            print(v, end=" ")

        # 인접한 정점 중에서 방문하지 않은 정점만 스택에 넣음
        for neighbor in reversed(graph[v]):  # LIFO 순서로 탐색
            if not visited[neighbor]:
                stack.append(neighbor)

# 그래프와 호출 예시
graph = {
    0: [1, 2],
    1: [0, 3, 4],
    2: [0],
    3: [1],
    4: [1]
}

dfs(graph, 0)
```
- 너비 우선 탐색(BFS, Breadth-First Search): 시작 정점을 먼저 방문하고, 그와 연결된 모든 정점을 방문, 큐를 이용
- 파이썬으로 구현한 BFS
```
from collections import deque

def bfs(graph, start):
    visited = [False] * len(graph)  # 방문 여부 기록
    queue = deque([start])  # 시작 정점을 큐에 넣고 시작
    visited[start] = True  # 시작 정점 방문 처리

    while queue:
        v = queue.popleft()  # 큐에서 정점 하나를 꺼냄
        print(v, end=" ")

        # 인접한 정점 중 방문하지 않은 정점 큐에 넣음
        for neighbor in graph[v]:
            if not visited[neighbor]:
                visited[neighbor] = True
                queue.append(neighbor)
# 그래프와 호출 예시
graph = {
    0: [1, 2],
    1: [0, 3, 4],
    2: [0],
    3: [1],
    4: [1]
}

bfs(graph, 0)
```

**트리(Tree)**
- 배열이나 연결 리스트를 이용해서 구현할 수 있음
- 서브트리(Subtree): 아래에 있는 노드들이 다시 트리 구조가 되는 구조
- 부모 노드(Parent Node): 임의의 노드 바로 위에 있는 노드
- 자식 노드(Child Node): 임의의 노드 바로 아래에 있는 노드
- 형제 노드(Sibling Node): 같은 부모 노드를 가지는 노드
- 레벨(Level): 루트 노드에서 임의의 노드까지 방문한 노드의 수
- 깊이(Depth): 트리의 최대 레벨
- 이진 트리(Binary Tree): 모든 노드들의 자식 노드가 두 개 이하인 트리, 배열이나 연결 리스트를 이용해서 구현 가능
- 완전 이진 트리(Complete Binary Tree): 이진 트리 중에서 단말 노드를 제외한 나머지 노드가 모두 두 개의 자식 노드를 가지고 있는 트리
- 포화 이진 트리(Full Binary Tree): 완전 이진 트리 중에서 모든 노드가 채워진 이진 트리
- 이진 트리 순회(Binary Tree Traversal): 이진 트리의 모든 노드를 특정한 순서대로 한 번씩 방문하는 것  
  - 전위 순회(Pre-order): 루트 -> 왼쪽 -> 오른쪽
  - 중위 순회(In-order): 왼쪽 -> 루트 -> 오른쪽
  - 후위 순회(Post-order): 왼쪽 -> 오른쪽 -> 루트
- 이진 탐색 트리(Binary Search Tree)   
  : 이진 트리 중에서 같은 데이터를 갖는 노드가 없으며, 왼쪽 서브 트리에는 현재 노드의 데이터보다 작은 데이터만, 오른쪽에는 큰 데이터만 있는 트리
  - 이진 탐색 트리의 균형 문제: 트리가 한쪽으로 치우쳐져 편향(Skewed) 상태가 되면, 트리의 높이가 매우 커져 비효율적인 연산이 발생
- AVL 트리(AVL Tree): 균형 이진 탐색 트리, 이진 탐색 트리의 한 종류
  - 균형 인수(BF, Balance Factor): 왼쪽 서브트리의 높이와 오른쪽 서브트리의 높이의 차이
  - 모든 노드의 균형 인수가 -1, 0, 1 사이로 유지되도록 균형을 맞춤
  - 자기 균형을 유지하며, 삽입이나 삭제 시 균형을 맞추기 위해 회전 연산을 수행
  - 검색 성능이 중요한 경우 적합
- 레드-블랙 트리 (Red-Black Tree): 각 노드에 색(빨간색 또는 검은색)을 부여하여 트리의 균형을 유지하는 자가 균형 이진 탐색 트리

**힙(Heap)**
- 완전 이진 트리를 기반으로 하는 자료구조
- 최소 힙(Min-Heap): 각 부모 노드의 값이 자식 노드들의 값보다 작거나 같음. 루트 노드는 최소값
- 최대 힙(Max-Heap): 각 부모 노드의 값이 자식 노드들의 값보다 크거나 같음. 루트 노드는 최대값
- 힙 생성 알고리즘(Heapify Algorithm)
  - i 인덱스의 왼쪽 자식 노드의 인덱스는 2i + 1
  - i 인덱스의 오른쪽 자식 노드의 인덱스는 2i + 2
  - 최대 힙을 만든다고 할 때, 특정 노드의 두 자식 중에서 더 큰 자식과 자신의 위치를 바꿈
- 우선순위 큐(Priority Queue), 힙 정렬(Heap Sort), 최소 신장 트리(Minimum Spanning Tree)에서 활용

**선택 정렬(Selection Sort)**
- 정렬되지 않은 데이터 중에서 가장 작은 데이터를 찾아 가장 앞의 데이터와 교환해나가는 방식
- 파이썬으로 구현한 선택 정렬
```
def selection(ds):
    for a in range(0, len(ds)-1):
        min_ind = a
        for b in range(a+1, len(ds)):
            if ds[b] < ds[min_ind]:
                min_ind = b
        ds[a], ds[min_ind] = ds[min_ind], ds[a]

dataset = [20, 50, 30, 10, 60, 40]
selection(dataset)
print(dataset)
```

**삽입 정렬(Insertion Sort)**
- 아직 정렬되지 않은 임의의 데이터를 이미 정렬된 부분의 적절한 위치에 삽입해가며 정렬하는 방식
- 시간 복잡도: 최선의 경우, $$O(n)$$, 최악의 경우, $$O(n^2)$$, 평균 시간 복잡도는 $$O(n^2)$$
- 파이썬으로 구현한 삽입 정렬
```
def insertion(ds):
    for a in range(1, len(ds)):
        key = ds[a]
        b = a-1
        while b>=0 and ds[b]>key:
            ds[b+1] = ds[b]
            b = b-1
        ds[b+1] = key

dataset = [20, 50, 30, 10, 60, 40]
insertion(dataset)
print(dataset)
```

**버블 정렬(Bubble Sort)**
- 서로 이웃한 데이터들을 비교해 가장 큰 데이터를 맨 뒤로 보내며 정렬하는 방식
- 시간 복잡도: 최선의 경우, $$O(n)$$, 최악의 경우, $$O(n^2)$$, 평균 시간 복잡도는 $$O(n^2)$$
- 파이썬으로 구현한 버블 정렬
```
def bubble(ds):
    for a in range(0, len(ds)-1):
        for b in range(0, len(ds)-1-a):
            if ds[b] > ds[b+1]:
                ds[b], ds[b+1] = ds[b+1], ds[b]

dataset = [20, 50, 30, 10, 60, 40]
bubble(dataset)
print(dataset)
```

**퀵 정렬(Quick Sort)**
- 기준키(Pivot)를 중심으로 작거나 같은 값을 지닌 데이터는 앞으로, 큰 값을 지닌 데이터는 뒤로 가도록 하여 분리해가며 정렬
- 피봇을 선택하는 방법: 첫번째나 마지막 값, 임의의 값, 중간값
- 시간 복잡도: 최선의 경우, $$O(nlogn)$$, 최악의 경우, $$O(n^2)$$, 평균 시간 복잡도는 $$O(nlogn)$$
- 공간 복잡도: $$O(logn)$$
- 파이썬으로 구현한 퀵 정렬
```
def quick(ds):
    if len(ds) < 2:
        return ds
    else:
        key = ds[0]
        left = [data for data in ds[1:] if data <= key]
        right = [data for data in ds[1:] if data > key]
        return quick(left) + [key] + quick(right)

dataset = [20, 50, 30, 10, 60, 40]
print(quick(dataset))
```

**선형 탐색(Linear Search)**
- 주어진 데이터 집합에서 원하는 데이터를 처음부터 순차적으로 비교하며 찾는 방법
- 시간 복잡도: 최선의 경우, $$O(1)$$, 최악의 경우, $$O(n)$$
- 파이썬으로 구현한 선형 탐색
```
def linear(ds, key):
    for a in range(0, len(ds)):
        if key == ds[a]:
            return a
    return

dataset = [20, 50, 30, 10, 60, 40]
print(linear(dataset, 10))
```

**이진 탐색(Binary Search)**
- 정렬된 데이터 집합을 반으로 쪼개 가면서 탐색하는 방법
- 시간 복잡도: 최선의 경우, $$O(1)$$, 최악의 경우, $$O(logn)$$
- 파이썬으로 구현한 이진 탐색
```
def binary(ds, key):
    low = 0
    high = len(ds)-1
    while low <= high:
        mid = (low+high)//2
        if key == ds[mid]:
            return mid
        elif key < ds[mid]:
            high = mid-1
        else:
            low = mid+1
    return

dataset = [10, 20, 30, 40, 50]
print(binary(dataset, 20))
```
