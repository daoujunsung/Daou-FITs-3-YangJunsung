# TIL: 서브쿼리, 윈도우 함수
## 학습 날짜: 2025년 1월 20일
## 오늘 내가 새롭게 배운 것들
**서브쿼리(Subquery)**
- SQL문 안에 포함된 또 다른 SQL문
- 메인 쿼리에서 값을 제공하거나 조건을 지정하는 역할
- 연관 여부에 따른 분류
  - 비연관 서브쿼리(Non-correlated Subquery)
    - 서브쿼리가 독립적으로 실행되어 결과를 반환
    - 메인 쿼리와 서브쿼리 간 상호작용 없음
    - 서브쿼리 단독으로 실행해도 실행 가능
    ```
    SELECT NAME
    FROM EMPLOYEES
    WHERE SALARY > (
        SELECT AVG(SALARY) FROM EMPLOYEES
    );
    ```
  - 연관 서브쿼리(Correlated Subquery)
    - 서브쿼리가 메인 쿼리의 각 행마다 실행
    - 메인 쿼리의 데이터를 서브쿼리에서 참조
    - 서브쿼리 단독으로 실행 불가
    ```
    SELECT NAME 
    FROM EMPLOYEES E1
    WHERE SALARY = (
        SELECT MAX(SALARY)
        FROM EMPLOYEES E2
        WHERE E1.DEPARTMENT_ID = E2.DEPARTMENT_ID
    );
    ```
- 위치에 따른 분류
  - 스칼라 서브쿼리(Scalar Subquery)
    - 서브쿼리가 컬럼 값으로 반환되어 SELECT 절에서 사용
    - 주로 집계 값이나 계산 결과를 포함할 때 활용
    ```
      SELECT NAME, SALARY,
        (
        SELECT AVG(SALARY)
        FROM EMPLOYEES
        WHERE DEPARTMENT_ID = E.DEPARTMENT_ID
        ) AVG_SALARY
    FROM EMPLOYEES E;
    ```
  - 인라인 뷰(Inline View)
    - FROM 절에서 서브쿼리의 결과를 테이블처럼 사용
    - 별칭을 붙여서 활용 가능
    ```
    SELECT D.DEPARTMENT_NAME, T.AVG_SALARY
    FROM DEPARTMENTS D,
        (
        SELECT DEPARTMENT_ID, 
            AVG(SALARY) AVG_SALARY
        FROM EMPLOYEES
        GROUP BY DEPARTMENT_ID
        ) T
    WHERE D.DEPARTMENT_ID = T.DEPARTMENT_ID;
    ```
  - 중첩 서브쿼리(Nested Subquery)
    - WHERE 절에서 조건을 설정하기 위해 서브쿼리 사용
    - 서브쿼리 결과에 따라 메인 쿼리의 필터링 결정
    - HAVING 절에서 조건을 설정하기 위해 집계 함수와 함께 활용
    ```
    SELECT NAME
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID = (
        SELECT DEPARTMENT_ID
        FROM DEPARTMENTS
        WHERE DEPARTMENT_NAME = 'Department 1'
    );
    
    SELECT DEPARTMENT_ID, SUM(SALARY) TOTAL_SALARY
    FROM EMPLOYEES
    GROUP BY DEPARTMENT_ID
    HAVING SUM(SALARY) > (
        SELECT AVG(SUM(SALARY))
        FROM EMPLOYEES
        GROUP BY DEPARTMENT_ID
    );
    ```
  - 반환값 유형에 따른 분류
    - 단일 행 서브쿼리(Single-row Subquery)
      - 서브쿼리가 단일 행과 단일 열을 반환
      - 비교 연산자와 함께 사용
      ```
      SELECT NAME
      FROM EMPLOYEES
      WHERE SALARY = (
          SELECT MAX(SALARY)
          FROM EMPLOYEES
      );
      ```
    - 다중 행 서브쿼리(Multi-row Subquery)
      - 서브쿼리가 여러 행이나 여러 열을 반환
      - 다중 행 연산자와 함께 사용
      - 메인 쿼리의 여러 열과 비교 가능
      ```
      SELECT NAME
      FROM EMPLOYEES
      WHERE DEPARTMENT_ID IN (
          SELECT DEPARTMENT_ID
          FROM DEPARTMENTS
          WHERE LOCATION = 'New York'
      );
      SELECT NAME
      FROM EMPLOYEES
      WHERE (DEPARTMENT_ID, JOB_ID) IN (
          SELECT DEPARTMENT_ID, JOB_ID
          FROM JOB_HISTORY
      );
      ```
- 사용 시 유의 사항
  - 성능 최적화
    - 비연관 서브쿼리는 독립적으로 실행되므로 더 빠름
    - 연관 서브쿼리는 메인 쿼리의 각 행마다 실행되므로 성능 저하 가능
  - 적절한 반환값 확인
    - 서브쿼리가 반환하는 값의 유형(단일 값, 다중 값)에 따라 연산자 선택이 중요
  - JOIN 대체 가능성
    - 일부 서브쿼리는 JOIN으로 대체할 수 있어 성능 개선 가능

**집합연산자**
- SQL에서 두 개 이상의 SELECT 결과를 결합하여 하나의 결과 집합을 생성
- 서로 다른 쿼리의 결과를 비교하거나 결합할 때 유용
- 종류
  - UNION
    - 두 SELECT 결과를 합치되 중복 제거
    - 결과는 정렬된 상태로 반환
  - UNION ALL
    - 두 SELECT 결과를 합치되 중복 포함
    - 결과는 정렬 X
  - INTERSECT
    - 두 SELECT 결과의 교집합
    - 중복된 행은 하나만 반환
  - MINUS
    - 첫 번째 SELECT 결과에서 두 번째 SELECT 결과를 뺀 차집합
    - 중복된 행은 하나만 반환
- 사용 시 유의 사항
   - SELECT 절에 나열된 컬럼 수와 데이터 타입이 동일해야 함
   - 결과 집합의 순서는 기본적으로 보장되지 않음
   - ORDER BY는 최종 결과에만 적용 가능
   - 대량의 데이터 처리 시 성능에 유의해야 함
   - ALIAS는 맨 처음 SELECT 절을 따름

**윈도우 함수**
- SQL에서 행(Row)을 기준으로 특정 범위(Window)를 정의하여 작업하는 함수
- 집계 함수와 함께 데이터를 그룹핑하지 않고도 계산 가능
- 순위 함수
  - RANK: 순위를 계산하며 동일한 값은 같은 순위를 부여 (순위 건너뜀)
  - DENSE_RANK: 순위를 계산하며 동일한 값은 같은 순위를 부여 (순위 건너뛰지 않음)
  - ROW_NUMBER: 순위를 고유하게 매김 (중복 없음)
  - 기본 문법
    ```
    SELECT 컬럼명,
      RANK() OVER (PARTITION BY 컬럼 ORDER BY 컬럼) AS rank,
      DENSE_RANK() OVER (PARTITION BY 컬럼 ORDER BY 컬럼) AS dense_rank,
      ROW_NUMBER() OVER (PARTITION BY 컬럼 ORDER BY 컬럼) AS row_number
    FROM 테이블명;
    ```
- 윈도우 집계 함수 기본 문법
```
SELECT 컬럼명,
  집계함수() OVER (PARTITION BY 컬럼 ORDER BY 컬럼) AS 결과컬럼
FROM 테이블명;
```
- PARTITION BY
  - 데이터를 특정 그룹으로 나눔
  - 그룹별로 윈도우 함수가 작동
- ORDER BY
  - 데이터를 정렬하여 윈도우 함수의 동작 순서를 지정
  - 누적 계산에 주로 사용
- 윈도우 함수 옵션: 특정 행이나 범위를 지정하는 추가 설정, 옵션을 활용해 데이터 처리 범위를 유연하게 조정 가능
  - 주요 옵션: ROWS, RANGE, UNBOUNDED PRECEDING, CURRENT ROW, FOLLOWING
