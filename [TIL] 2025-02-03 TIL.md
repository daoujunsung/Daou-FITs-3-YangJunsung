# TIL: SQL 프로젝트
## 학습 날짜: 2025년 1월 31일
## 어떤 문제가 있었는지
- 계산 쿼리를 작성함에 있어 문제가 발생했다.
- 작성자 별로 평점이 가장 높은 리포트를 보여주는데 만약 평점이 같다면 조회수가 더 높은 것을 보여주는 것을 짜야 했다.
## 그 문제를 해결해보기 위해 내가 시도해본 것들
- alias를 지워보았지만, 해결되지 않았다.
- 정수를 곱하지 않고, .0을 붙여 실수 형태로 나타내어보았다.
- group by를 써보았지만, 기준을 2개를 모두 사용할 수 없었다.
- ROW_NUMBER() 윈도우 함수를 사용하고 이를 인라인 뷰에 사용하였다.
## 어떻게 그 문제를 해결했는지
- 실수로 나타냈을 때 문제가 해결되었다.
- ROW_NUMBER() 윈도우 함수를 사용하여 원하는 기준 2개를 모두 적용할 수 있었고, 이를 작성자 테이블과 조인하여 작성자 이름 별로 가장 높은 평가를 받은 리포트를 보여줄 수 있었다.
## 오늘 내가 새롭게 배운 것들
- 정수 간의 연산의 결과가 정수로 나올 수 있어 오류가 발생했다. 그럴 땐, 임의로 1.0을 곱해주는 등 실수로 형변환을 해주면 된다.
- 여러 가지 기준을 동시에 이용해야 할 때는 group by와 order by를 통해 랭크를 매기는 것이 아닌 윈도우 함수를 사용하는 것이 효과적이다.
